# CHANGELOG

작업 기록 정본. 한 일은 자신의 이름(Claude · Codx · Gemini)으로 남긴다(「작업 기록」 규칙). 핵심 결과와 함께 **기각 사유**도 기록한다 — 이미 기각된 길을 다시 파지 않기 위해서다.

## 2026-09-02

- 이미지 편집기에 Group 없이 단일 Fabric 객체로 둥근 사각형·삼각형·별·육각형·말풍선·단/양방향 화살표·형광펜을 추가했다. fill·stroke·외곽선 폭 적용 행렬을 도형 종류 메타데이터로 고정하고 형광펜은 fill만 허용하며 opacity 0.45를 직렬화했다. 화살표 외곽선 폭 4→18 변경 전후 polygon points·width·height가 동일함을 단위·브라우저 스모크에서 확인했다. SVG 스티커 후보 스파이크는 Twemoji v17.0.3(4,009개, 10,121,593B, 개별 gzip 합 4,475,637B)와 Noto Emoji v2.051(3,731개, 32,128,362B, 11,225,395B)을 경로별 라이선스 원문까지 비교해 Twemoji를 채택하고 Noto는 원시 3.17배·gzip 2.51배 규모 때문에 기각했다. 상한 120종 중 7카테고리 112종을 코드포인트·바이트·SHA-256 manifest로 고정했으며 실제 합계는 142,436B/개별 gzip 71,573B다. 별도 벤더 스크립트가 dev/prebuild에서 고정 commit 자산과 CC BY 4.0 원문을 검증하고, 생성 라이선스 번들과 dist 검사가 이를 다시 확인한다. 피커는 카테고리·ko/en 검색과 개별 정적 SVG URL만 사용해 선택 스티커를 이미지 레이어로 삽입하며 기존 텍스트 입력의 이모지는 유지했다. Image Studio lazy chunk는 356.23→384.87KB, gzip 109.76→120.86KB로 늘었고 SVG 본문은 포함하지 않는다. ko/en 화면·가이드·도구/SEO 문구와 390×844 모바일 피커를 반영했으며 URL·광고 배치·격리 경로는 바뀌지 않아 별도 AdSense 변경은 하지 않았다. — Codx
- 무접두사 `/tools/excel-merger/xls-preserve/`도 ko 작업 화면과 같은 정적 문서·문서 범위 준비 스크립트를 산출하도록 보완했다. noindex·사이트맵/hreflang 제외·한국어 Excel 가이드 canonical·광고 제외 정책을 정적 검사에 고정해 ko·en 산출물과의 불일치를 제거했다. — Codx
- 전역 격리 헤더가 없는 GitHub Pages 동형 서버에서 표준 Excel 화면의 전역 SW 제어 뒤 XLS 보존 화면으로 처음 이동하면, 문서는 `COEP: require-corp`로 격리되지만 `/assets/excel.worker-*.js` 응답은 무헤더라 Chrome이 `ERR_BLOCKED_BY_RESPONSE`로 차단하고 합성 파일 4개가 모두 “파일 처리를 시작하지 못했습니다”로 끝나는 기전을 확정했다. same-origin `worker`·`sharedworker` 응답에 `COEP: require-corp`·`CORP: same-origin`을 주입해 첫 진입 즉시 검사를 복구했고, `credentialless` 비디오 문서에서도 실제 메타데이터 워커 기동이 호환됨을 확인했다. 문서 격리 준비는 활성화 후 최대 한 번만 자동 리로드하도록 세션 가드를 추가했다. ko·en 보존 정적 페이지는 `332d8f7`부터 이미 생성·검증되고 운영도 착수 시 200이어서 “현행 생성기 상수만 존재·운영 404” 가설은 기각했으며, 의도대로 noindex·사이트맵/hreflang 제외·언어별 Excel 가이드 canonical·광고/분석 제외를 강화 검증했다. 무헤더 서버·실제 SW·404 폴백·합성 XLSX/비디오 fixture 전용 스모크를 추가했고 dummyfortest는 사용하지 않았다. — Codx
- 이미지 편집기의 반응형 화면 맞춤은 CSS-only로 유지하고, 사용자 보기 전용 25–400% 확대·축소와 이동을 별도 캔버스 보기 상태로 추가했다. 데스크톱은 휠 확대와 Space+드래그 이동, 모바일은 capture 단계에서 두 번째 손가락을 감지해 진행 중 개체 변형·그리기·영역 선택을 취소한 뒤 두 손가락 pinch/pan으로 전환하며 한 손가락 개체 조작은 유지한다. 파일 교체·빈 캔버스·비율/자유 crop·치수 변경 undo/redo는 100% 화면 맞춤으로 초기화하고, 치수 불변 undo/redo는 보기를 보존한다. 미니바 위치에는 사용자 확대·이동을 반영했으며 PNG/JPG/WebP 내보내기는 보기 변환을 잠시 제거한 뒤 복구해 효과와 overlay 포함 여부까지 픽셀 결과가 보기에 좌우되지 않게 했다. 강도 계산은 `getObjectScaling()+getZoom()`만 유지하고 DPR 1·2 × 100·200%에서 각각 같은 16px·8px 소스 블록을 실측했다. 보기 변환을 반응형 fit이나 편집 기록에 섞는 안, DPR을 재유입하는 전체 스케일 계산은 각각 의미 충돌·강도 회귀 때문에 기각했다. ko/en 조작 문구와 가이드를 갱신했으며 기능 URL·검색 설명·정적 페이지 구조·광고 배치·광고 격리 경로에는 영향이 없어 SEO·AdSense 변경은 하지 않았다. I4·I5 자산 확충은 계획대로 범위 밖이다. — Codx
- SpreadsheetML 위장 `.xls`를 SheetJS로 읽기 전에 CDATA 섹션을 XML 이스케이프 텍스트로 전개해, CDATA 263개가 든 실파일도 `AC285`·`A1:L108`·108행으로 읽고 4개 실파일 배치 병합까지 지원했다. `<`·`>`·`&` CDATA를 포함한 전각·한글·공백 이름의 합성 SpreadsheetML+XLSX 혼합 스모크에서 시트 목록과 병합 결과 값을 검증했으며, 정상 XLSX/OLE XLS/CSV 경로는 유지하고 전처리 후에도 실패하는 비정상 XLML만 기존 파일별 XLSX 재저장 안내로 남겼다. 모든 `.xls`에 문자열 파싱을 적용하는 안은 기존 OLE 경로 회귀 위험 때문에 기각했다. 사용자 기능 의미·색인 경로·정적 페이지·광고 배치·광고 격리 경로는 바뀌지 않아 별도 SEO·AdSense 변경은 불필요하다. — Codx
- Excel XLS 보존 경로를 확장자 대신 OLE compound 시그니처(`D0 CF 11 E0`)로 판정해 SpreadsheetML 위장 `.xls`는 일반 검사로 보내고, OLE 변환 실패는 원본 파일명이 포함된 ko/en 안내와 함께 해당 파일에만 격리했다. 변환 FS·URL 이름은 `input-N.xls`/`output-N.xlsx`로 제한하고 명령 브리지의 ASCII 계약과 생성된 벤더 사본 동일성 검증을 추가했으며, 전각·한글·공백 SpreadsheetML+XLSX 혼합 및 OLE 개별 실패 스모크를 보강했다. F0 실파일 4개에서 고정 ZetaOffice는 두 XML을 모두 열고 변환·4파일 병합까지 성공해 “ZetaOffice SpreadsheetML 미지원”과 전각 파일명 단독 원인 가설은 기각했다. SheetJS 0.20.3은 `AC285_202606.xls`를 읽지만 `AC285_20260８５８6.xls`는 XLML 태그 오류로 실패해 파일별 지원 편차로 판정했으며, 수정 후에는 후자만 XLSX 재저장 안내가 나오고 나머지 3개 병합이 성공했다. OLE 헤더만 붙인 빈 fixture는 ZetaOffice가 정상 변환해 실패 fixture로 기각했다. 기능 의미·색인 경로·광고 배치·광고 격리 경로는 바뀌지 않아 SEO·정적 페이지·AdSense 변경은 불필요하며 벤더 바이너리 스냅샷도 변경하지 않았다. — Codx
- 이미지 스튜디오 편집기를 기존 Fabric 상호작용 모드와 8개 UI 패널 상태로 분리하고, 상단 툴바·sticky 캔버스·데스크톱 우측 옵션 패널·390px 모바일 하단 시트로 재구성했다. 그리기 패널은 마지막 연필/붓/지우개·색·굵기를 복원하고 텍스트·도형·스티커 삽입 뒤에도 해당 패널을 유지한다. 선택 개체 옆 미니바에 색·굵기·맨 앞/맨 뒤·복제·삭제를 추가해 이동·회전·배율·리사이즈와 CSS-only fit을 추종하게 했으며, 원본+영역 효과는 기존 원자 레이어 재정렬을 유지하고 원본 복제·삭제와 효과 개체 선택은 차단했다. ko/en 문구와 안정적 aria/data-testid 스모크, 390×844의 44px 타깃·하단 시트·상태 복원·미니바 추종 검증을 반영했다. `src/app/seo.ts`·ko/en `tools.json`·이미지 가이드는 기능 의미가 이미 정확해 변경 불필요, 정적 페이지·광고 배치·격리 경로 영향 없음으로 판단했다. 광고 공간을 침범하는 뷰포트 전체 고정은 기각했고 I2 줌·팬과 I4/I5 자산 확충은 이번 디스패치 범위 밖이라 구현하지 않았다. — Codx
- Excel 시트 선택기를 가용 폭 기준 1~2열 카드 그리드로 바꾸고, custom 모드는 `aria-pressed` 버튼 칩·all/positions 모드는 비인터랙티브 상태 칩으로 분리했다. 파일 카드 heading/전체 이름 접근성·ellipsis·키보드 focus ring, 목록 전용 내부 스크롤, 모바일 44px 타깃과 Step 2 sticky 파일·시트 요약을 ko/en으로 추가했으며 6개 파일·24개 시트 fixture에서 열 수·높이·821px overflow·모바일 1열·모드별 상호작용을 계측하는 스모크를 보강했다. 기능 의미가 바뀌지 않아 SEO·정적 페이지 변경은 불필요하고 광고 배치·격리 경로 영향도 없음으로 판단했다. — Codx
- VP9 인코딩에 `-deadline good -cpu-used 4`를 명시하고 전체 인자 배열 회귀 테스트와 고정 벤치 fixture·스크립트를 추가. warm-up 뒤 3회 host libvpx 중앙값이 7,882.015ms→2,462.353ms로 68.76% 감소(3.201×), 출력은 672,326→720,187 bytes(+7.12%), SSIM 0.971567→0.971193·PSNR 28.713933→28.708202dB, 양쪽 전체 디코드 통과. 브라우저 FFmpeg.wasm VP9은 기존 런타임에서 작은 fixture도 메모리 오류가 나 별도 런타임 결함으로 기록했다. B1b에서는 zip.js 2.9.0의 순차 Blob 스트림·강제 ZIP64·외부 unzip 왕복을 통과했지만 Mediabunny 1.55.5 trim은 B-frame fixture duration이 FFmpeg보다 2프레임 길어 B2 후보에서 기각하고 mp4box.js+고정 mp4-muxer 대안으로 회귀했다. 사용자 문구·SEO·정적 페이지·광고 격리 경로 변경 없음. — Codx
- Gemini 위임 경로를 확립: 이 호스트의 Gemini 호출은 agy CLI로 수행하며(`--add-dir` 필수 — 없으면 저장소 파일 접근 불가 실측), 타임아웃 상향·`--json-schema` 구조화·`--sandbox` 사용 기준과 산출물 검증 절차를 `CLAUDE.md` 「호스트 참고」에 기록. — Claude
- Chrome 기반 앱 아이콘 생성기를 추가해 SVG 그라데이션을 8-bit RGBA PNG 5종으로 재생성하고 manifest의 `any`/`maskable` 자산을 분리했다. 그라데이션 미지원 래스터라이저 재사용과 투명 라운드 아이콘의 Apple·maskable 겸용은 각각 색상 소실·마스크 크롭 문제로 기각했으며 픽셀·알파 검증, 빌드, 단위 테스트 58개, 정적 산출물 검증을 통과했다. — Codx
- 세 에이전트 협업 체계 도입: 공통 규칙 정본 `PROJECT_RULES.md` 신설(기존 CLAUDE/AGENTS 공통 3규칙을 「GitHub Pages 스택」·「현지화·SEO·AdSense 동시 검토」·「내부 구현 비노출」로 명명해 이관), 역할 지시서 `CLAUDE.md`(오케스트레이터·판정자) · `AGENTS.md`(Codex, 코드 유일 작성자) · `GEMINI.md`(대규모 컨텍스트 분석·조사) 분리. `docs/jobs/` 오프라인 작업지시서 체계와 `docs/backlog.md` 도입. 사용자 결정으로 「커밋·업로드·배포는 Codex」 규칙 추가. — Claude
- 운영 문서를 규칙 정본의 부속 명세로 연계: 「생성물 직접 수정 금지」에 벤더 스냅샷 해시 검증 원칙과 `docs/OFFICE_EDITOR_ASSETS.md` 참조, 「현지화·SEO·AdSense 동시 검토」·「커밋·업로드·배포는 Codex」에 `docs/PUBLISHING_CHECKLIST.md` 참조를 추가. 수치·절차는 규칙이 아니라 해당 문서에서 갱신한다. — Claude
- 2026-08-15–16 신뢰성 작업지시서 4개를 종결하고 `docs/jobs/archive/` 오프라인 아카이브로 이관. 1차는 비디오·오디오·이미지·PDF·데이터·문서·계산/보안 전 영역의 약 120개 항목을 감사해 P0 데이터 무결성 결함과 주요 P1을 수정했고, 2차는 수정 회귀와 부분 해결분을 재검증했으며, 3차는 R1–R14 해결과 블러·비디오 회귀 S1–S14를 추적했고, 4차는 S1–S14 해결을 확인했다. — Codx
- 3차 이의 제기 판정 보존: 규칙 출처 표기는 일부 수용, `w:trackRevisions` 삭제 지시는 정식 OOXML 요소라는 증거로 기각하고 잘못된 `trackChanges`만 제거, 오디오 워커의 파일 전환 종료는 메모리 정리 정책상 유지, Excel 끝단 트림 안전장치 완화는 불변식 검증 전까지 기각, 비디오 memo 콜백 비교는 의도적 무시 계약을 주석·테스트로 확정했다. — Codx
- 현행 `72981cc`에서 4차 T1–T7을 재검증: T1·T2·T3·T5·T6·T7 해결, T4 미해결. 근거는 `rg` 실측으로 T1의 `getObjectScaling`과 DPR 비의존 단위 테스트, T2의 유효 구간 기반 export readiness와 60초 probe timeout, T3의 `frameRateProbeStatus` 1회 완료 가드, T5의 base 위 효과 레이어 재정렬과 Safari 2–3 pass 폴백, T6의 선택 영역 소스 픽셀 렌더(효과 경로 전체 `toCanvasElement` 없음), T7의 원본 샘플링 주석·ko/en 가이드·좌표/memo 테스트·시트 참조 인덱스 1회 구축을 확인했다. T4는 `resolveConcatFrameRate(..., fallback = 30)`과 `[undefined, 0, NaN] -> 30` 테스트가 남아 있어 `docs/backlog.md`로 이관했다. FPS 필터를 단순 생략하는 안은 재인코딩 세그먼트를 stream-copy 결합하는 현재 구조에서 공통 호환 레이트를 잃으므로 기각했다. — Codx
- 종결 검증: `npm run build` 통과(2,336 modules transformed, 55 localized crawlable pages generated), `npm run test:unit` 통과(15 tests, 15 pass, 0 fail), `npm run test:static` 통과(localized pages·hreflang·self-hosted runtimes·ads.txt·robots.txt·sitemap.xml). — Codx
