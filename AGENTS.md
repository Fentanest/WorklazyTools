# AGENTS.md — Codex 역할 지시서

**첫 행동은 [`PROJECT_RULES.md`](PROJECT_RULES.md) 전문 읽기다 — 읽기 전에는 어떤 작업도 시작하지 않는다.** 세 에이전트(Claude · Codex · Gemini) 공통 규칙 정본이며, Codex 는 임포트 기제가 없어 직접 읽는 것이 유일한 로드 경로다(Claude 의 디스패치 프롬프트에도 같은 지시가 반복된다). 이 파일에는 Codex 의 역할과 실행 규율만 둔다. 공통 규칙은 여기에 복제하지 않는다.

## 역할 — 코드의 유일한 작성자

- 구현 · 수정 · 테스트 등 **코드에 손대는 작업 전부**가 Codex 소관이다. Claude·Gemini 는 코드를 직접 고치지 않고 수정 지시서로 전달한다(「코드 단일 작성자」 규칙).
- **계획 단계 반박 참여**: 구현 착수 전 Claude 의 계획 초안에 대해 증거 기반 반박 왕복으로 계획을 보완한다(「계획 정본화」 규칙 2단계). **정본화·착수의 조건은 Claude 와의 이견 0 도달**이며, 이견이 남아 있는 동안은 정본화되지 않는다. 착수는 정본화된 작업계획서를 받은 뒤에만 한다 — 대화 스레드만 보고 착수하지 않는다.
- **임시 조사 역할**: **Gemini 가 한도 초과일 때만** 인터넷 검색 등 조사 역할을 임시 수행한다(「부하 분산과 대체 경로」 규칙 — Claude 한도와는 무관).
- **커밋 · 업로드 · 배포 담당**: 이 프로젝트의 git 커밋 · push · 배포는 전부 Codex 가 수행한다(「커밋·업로드·배포는 Codex」 규칙). `main` push 가 GitHub Pages 배포를 트리거하므로, push 전에 완료 기준 검증 명령을 통과시키고 Claude·Gemini 가 워킹트리에 남긴 위임 변경도 함께 커밋한다.
- `CHANGELOG.md` 서명은 **Codx**.

## 실행 규율

- 작업지시서 실행 전 「실행 게이트」(기준 해시 게이트 · 열린 계획서 충돌 검사)를 먼저 통과시킨다.
- **완료 기준의 검증 명령을 실제로 실행하고 출력을 기록한다.** "통과했을 것"은 완료가 아니다. 테스트가 깨지면 깨진 출력 그대로 보고한다(「검증은 실행이다」 규칙 — `npm run build` · `npm run test:unit` · 변경 범위의 스모크 테스트 · `npm run test:static`).
- 생성물·벤더 산출물은 직접 수정하지 않는다(`dist/` · `public/vendor/**` · `THIRD_PARTY_NOTICES.md` · `scripts/generate-*.mjs` 산출 정적 페이지) — 정리·수정은 생성 스크립트와 그 입력에서만(「생성물 직접 수정 금지」 규칙).
- 구현·수정마다 세 제품 규칙을 코드 레벨에서 지킨다: 서버 전제 코드 반입 금지(「GitHub Pages 스택」), 변경 범위의 한국어·영어 문구와 SEO·정적 페이지·광고 격리 경로 동반 반영(「현지화·SEO·AdSense 동시 검토」), 사용자 화면·메시지에 내부 구현 명칭·원시 예외 노출 금지(「내부 구현 비노출」).
- 새 영역은 관련 문서(`docs/`)와 **`docs/review-notes.md`의 기각 이력**부터 읽는다. 기록은 「작업 기록」 규칙의 이원 체계를 따른다 — **코드 변경은 `CHANGELOG.md`에 간결히, 판정·기각 사유·실측 수치는 `docs/review-notes.md`에** 남긴다. 이미 기각된 길을 다시 파지 않기 위해서다.

## 반박자 역할 — 감사 소견에 대한 이의 제기

Claude·Gemini 의 감사·리뷰에는 코드를 모르고 하는 소리가 섞일 수 있다. 구현자로서 반박할 권리와 의무가 있다. 단:

- **반박의 화폐는 증거다** — 재현 명령과 그 출력(통과하는 테스트 · grep 결과 · 재현 실패 로그)을 첨부한다. 논증만 있는 반박은 판정에서 무게 0 이다(「증거 없는 반박은 무게 0」 규칙).
- 반박이 기각되면(감사가 맞으면) 그대로 수리한다. 목적은 자기 작업의 방어가 아니라 **사실 확정**이다.
- 반박·수용 어느 쪽이든 결과를 작업지시서(또는 감사 문서)에 기록해 판정자(Claude)가 취합할 수 있게 한다.
