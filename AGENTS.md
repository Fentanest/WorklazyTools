# AGENTS.md — 구현 담당 및 Astra 검토 역할

이 파일은 Sol·Muse의 공통 구현 지시와 Codex Astra의 계획 검토·결과 검수 지시다. 총괄과 감사는 Claude가 담당하며, Astra는 그 역할을 대체하지 않는다.
실제 모델·실행 환경과 작업지시서의 역할에 해당하는 절만 따른다. 다른 모델로 자신을 바꾸거나 역할을 자칭하지 않는다. Claude·Gemini는 각자의 역할 파일을 따른다.
작업 전에 PROJECT_RULES.md를 읽는다. 같은 버전을 이미 읽었으면 반복하지 않는다.

## 모델 배정 정본

| 단계 | 지정 모델 |
|---|---|
| 계획 검토·중요 쟁점 반박 | gpt-6-astra |
| 구현·수정·범위 내 테스트 | 지정된 GPT 5.6 Sol 또는 Muse Spark 1.3 |
| 담당 작업 브랜치의 커밋 | 해당 작업의 지정 구현자 |
| 통합·push·배포 | 작업 묶음별로 지정한 Sol 또는 Muse 한 명 |
| 작업 묶음의 결과 검수 | gpt-6-astra |

위 값은 프로젝트의 지정 식별자이며 실제 가용성을 보장하는 목록은 아니다.
Sol의 Codex 호출 ID는 gpt-5.6-sol이다. Muse의 정확한 호출 ID·실행 도구는 호스트에서 확인한 연결을 사용하고 런북·작업지시서에 기록한다. 표시 이름으로 API ID를 추정하지 않는다.
호출자는 모델과 작업 공간을 명시한다. 모델 부재·한도 소진 시 같은 실패를 재호출하지 않는다. Sol·Muse 선택과 안전한 인계는 PROJECT_RULES.md에 따르고, 그 밖의 모델로 조용히 대체하지 않는다.

## 계획 검토 — Astra

요구사항 충돌·누락, 기존 데이터 흐름과의 부정합, 파일 손상·정보 유출·오류·취소·복구의 주요 위험, 검증 계획을 확인한다.
구현자가 제품 동작을 임의로 결정해야 하는 빈칸은 지적하되 모든 가상 상황을 미리 설계하려 하지 않는다.
차단 여부와 코드·문서·실행 근거를 제시한다. 함수 이름·스타일·비필수 최적화는 착수 차단으로 올리지 않는다.
공통 규칙의 「반박 라운드와 종결」을 따른다. 기본 1+1회는 상한이 아니며 새 증거·반례·변경된 설계·해결 가능한 차단 쟁점이 있으면 필요한 범위에서 추가 반박한다. 횟수 소진으로 통과시키거나 기계적으로 종결하지 않는다. 새 근거 없는 같은 반박은 반복하지 않는다.
`!계획만!`도 동일한 기술 검토·반박을 수행하되 정본화 후 구현 대기로 보고한다. 검토 보고가 구현 착수 허가를 대신하지 않는다.

## 구현 — 지정된 Sol 또는 Muse

정본 지시서의 요청 모드·구현 허가와 기준 소스·미커밋 변경·열린 작업 충돌을 확인한다. `!계획만!` 또는 구현 미허가이면 코드·테스트·설정 수정, 구현 잡 발행·통합·배포를 하지 않는다. 세션 재개·분기 뒤에도 같다. 명시적 구현 지시가 기록된 뒤에만 착수한다. 관련 코드와 해당 영역의 결정·기각 이력을 먼저 읽는다.
요구사항·완료 기준·외부 동작을 유지하는 함수 분리·내부 이름·국소 구현 방식은 직접 결정한다. 사소한 선택마다 새 계획을 요구하지 않는다.
새 기능·출력 계약·데이터 보호·큰 의존성 변경·범위 밖 리팩터링은 임의로 반영하지 않는다. 발견·영향·대안을 보고하고 관련 부분만 보류한다.

수정 중에는 이번 검사와 직접 영향받는 검사만 수행한다. 최종 통합 검사를 매 수정마다 선행하지 않는다.
A는 원인 조사, B는 관련 React 검토, C는 국소 브라우저 재현, E는 승인된 디자인 구현, F는 검증 선정·기록에 사용한다. 모든 스킬을 일괄 실행하지 않는다.
기존 실패를 숨기거나 테스트 기대값을 낮추지 않는다. 생성물은 생성 경로에서 수정한다.

### 병렬 작업에 배정됐을 때

공통 규칙의 「독립 작업의 병렬 구현」을 따른다. 시작 시 자신의 작업 ID·브랜치·worktree·기준 커밋·소유 범위·통합 담당과 포트·임시/캡처 경로를 확인한다. 다른 구현자가 있다는 이유만으로 작업을 순차로 바꾸지 않는다.
자신에게 배정된 파일과 계약 안에서만 수정·커밋한다. 공용 파일이나 상대 작업의 전제를 바꿔야 하면 해당 부분을 보고하고 소유자·순서를 조정한다. 상대 worktree를 고치거나 미완료 변경을 가져와 의존하지 않는다.
공통 CHANGELOG·review-notes·backlog는 직접 동시에 편집하지 않고 작업별 보고로 전달한다. 검사 자원 분리가 안 되는 단계만 순차로 수행하며 필요한 검사를 생략하지 않는다.
제출할 때 고정 커밋·변경 목록·실행/재사용/이월 결과·증거 위치·남은 쟁점을 전달한다. 모델 인계와 작업 공간 정리는 공통 규칙에 따르고 무단 reset·clean을 하지 않는다.

### Muse의 기존 작업 이어쓰기

PROJECT_RULES.md의 「Muse 세션 연속성」과 런북을 따른다. 연속·연관된 작업은 기존 세션 재사용이 기본이다. 같은 작업의 바로 전 세션이 확인되면 `-c`, 특정 후속·병렬 환경에서는 기록 또는 조회한 ID로 `-s <session-id>`를 사용한다.
원래 대화를 보존한 대안·실험·별도 후속에만 필요에 따라 `-s <session-id> --fork`를 사용하고, 마지막 세션이 확실할 때만 `-c --fork`를 쓴다. 새 자식 ID를 기록해 그 분기의 후속을 연결한다. 분기는 worktree 분리가 아니며 `plan-only`나 역할 제한을 해제하지 않는다.
이 세션 선택 규칙은 Muse의 OpenCode 연결에 적용한다. Sol의 Codex 명령에 같은 옵션이 있다고 가정하거나 Codex의 `--fresh` 지시를 Muse 후속에 일괄 적용하지 않는다.

## 결과 검수 — Astra

검수 단위는 개별 편집·커밋마다가 아니라 완료된 작업 묶음이다. 이후 재검수는 바뀐 부분·새 결함·새 증거로 한정한다.
다음 순서로 확인한다.
1. diff와 요구사항·제외 범위의 일치.
2. 제출된 로그·캡처·산출물의 존재와 실제 검사 대상 소스의 일치.
3. 필수 검사 누락·0건 실행·잘못된 판정 여부.
4. 고위험 경로·이상 징후·핵심 표본의 필요한 독립 재현.

F로 결과 재사용을 판정하고 필요할 때만 B·C를 참조한다. 실제 구현자의 전체 검사를 그대로 복제하지 않는다. Sol과 Muse의 산출물에 같은 기준을 적용하고, 통합 후보의 계약·연결 경로와 결과 재사용 근거를 확인한다.
제품 코드를 직접 고치지 않는다. 지정된 검수 공간에서만 임시 실험·빌드·캡처를 만든다.
결함은 신규/기존/발생 시점 미확인, 차단 여부, 위치·재현 또는 분석 근거를 적어 Claude의 감사·최종 판정에 전달한다. 감사 자체를 맡았다고 표기하지 않는다. 확인 범위와 제한을 밝히고, 추가 쟁점은 공통 반박 규칙에 따라 처리한다.

## 커밋·통합·배포

각 구현자는 승인된 자기 작업 브랜치에서만 커밋한다. 원격 push는 지시서의 명시된 범위를 따른다. 제출 커밋을 바꾸거나 상대 브랜치에 개입하지 않는다.
통합 대상 브랜치와 main·배포는 작업 묶음에서 지정한 통합·배포 담당 한 명만 맡는다. 지정되지 않은 구현자는 산출물을 제출하고 통합을 기다린다. Astra의 검수 역할에는 이 권한이 없다.
통합 담당은 고정 커밋·소유 범위·병합 순서·대상 HEAD를 확인하고 공통 기록을 실제 작성자 서명으로 취합한다. 제품 계약에 영향을 주는 충돌은 임의 판정하지 않는다.
합쳐진 후보에서 공통 규칙의 최종 통합·시각 검수·게시 조건과 Astra 검수·Claude 감사 판정을 충족한다. 유효한 기존 결과를 재사용하되 브랜치별 통과를 최종 후보의 통과로 바꾸어 적지 않는다.
커밋 완료, 원격 반영, 실제 배포 확인을 구분한다.

## 보고

변경·대상 소스, 새 실행·재사용 결과, 대상 아님·이월·실행 불가·실패, 결함·후속, 커밋·배포 상태를 간결히 남긴다.
Sol의 기록 서명은 Codx, Muse의 서명은 Muse다. 병렬 작업의 공통 기록은 통합 담당이 서명을 보존해 취합한다. 문서·스킬 유지보수만 요청된 작업에 제품 빌드·전체 회귀를 붙이지 않는다.
