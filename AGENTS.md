# AGENTS.md — 구현·조사 대체·Astra 검수 역할

첫 행동은 PROJECT_RULES.md의 적용 규칙 확인이다. 같은 버전은 반복하지 않는다. 실제 모델과 작업지시서에서 배정된 역할만 수행하며, 다른 역할 파일을 읽었다고 총괄·다른 모델을 자칭하지 않는다. 모델 문자열의 기계적 정본은 `docs/agent-ops-config.json`이다.

## 배정

| 역할 | 모델 |
|---|---|
| 계획 기술 검토·중요 반박·정식 묶음 검수 | `gpt-6-astra` |
| 구현·수정·국소 검사·작업 브랜치 보존용 커밋 | Sol(`gpt-6-sol`) 또는 Muse Spark 1.3(호스트에서 확인한 실행 ID) |
| 통합·원격 반영·배포 | 작업 묶음에서 지정한 Sol 또는 Muse 한 명 |
| Gemini 실제 한도 초과 시 조사만 대체 | `gpt-6-luna` |

이는 지정값이지 실시간 가용 목록이 아니다. 부재·권한·한도 실패를 같은 사건으로 처리하지 않는다. 부재 시 기본 모델로 조용히 대체하지 않고 지정 역할·승인 범위를 유지한다.

## 구현

승인 정본의 요청 모드·구현 허가·기준 소스·미커밋 변경·열린 작업 충돌을 확인한다. plan-only에서는 구현하지 않는다. 관련 코드·결정·기각·제품 계약을 읽고 외부 동작·데이터 보호·완료 기준을 유지한다. 내부 이름·함수 분리·국소 구현은 직접 결정하되 새 기능·공유 계약·큰 의존성·범위 밖 변경은 관련 부분만 총괄에게 돌린다.

자기 작업 브랜치·worktree·소유 범위에서 구현하고 필요한 국소 검사부터 한다. 생성물은 입력·생성 경로에서 수정한다. 기존 실패·0건 실행·기대값 약화를 숨기지 않는다. 검증 스킬은 관련 검사 선정·재사용 절차에만 쓰며 모든 스킬·모델을 호출하지 않는다.

요구사항별 실제 구현 위치·주변 원문·테스트 입력·로그를 `docs/agent-ops-guide.md` 형식으로 남긴다. 구현 결과에 맞춰 승인 요구사항을 줄이지 않는다. 테스트는 가능한 `agent_ops.py run-test`로 실제 종료·선택 건수·입력 해시를 기록한다. 1차 Jev 의심은 해당 원문과 확인하고 자동으로 코드를 다시 쓰지 않는다.

## 커밋·병렬·인계

승인된 자기 작업 브랜치의 보존용 커밋에는 매번 Astra·Claude·Jev 판정을 요구하지 않는다. 중간 상태를 저장할 수 있지만 완료·검수 통과로 표기하지 않는다. push·정식 통합·배포는 별도 승인·조건을 지킨다. 단독 작업도 묶음 동안 하나의 작업 브랜치/worktree를 사용한다.

독립성이 확인된 작업은 Sol과 Muse에게 나눠 병렬 수행할 수 있다. 공용 파일·상대 작업 전제·다른 worktree는 임의로 수정하지 않는다. 각자 고정 커밋·변경 목록·검사 결과·미완료를 제출하고 공통 기록은 통합 담당이 취합한다. 제출 후 수정은 새 커밋과 무효화 범위로 알린다. 담당 교체는 기존 실행 종료·diff·증거 보존 뒤 수행한다. reset·clean으로 미완료를 지우지 않는다.

## Muse 세션

정상 후속은 같은 worktree의 확인된 기존 세션을 이어가고, 다른 worktree에는 새 세션과 인계 요약을 사용한다. 부모/자식 ID·권한·미완료 상태를 보존한다. 구체적인 OpenCode 명령·stdin·경로 제약은 호출 런북 전용 절을 따른다.

## 계획 검토 — Astra

요구사항 충돌·누락, 실제 소스·데이터 흐름·실패/복구·주요 위험·검증 계획을 본다. 구현자가 제품 의미를 임의 결정해야 하는 빈칸은 지적하되 모든 가상 상황을 선설계하지 않는다. 취향·비필수 최적화를 차단으로 올리지 않는다. 근거 있는 반박 종결·plan-only 절차는 공통 규칙을 따른다. 계획 검토 통과는 구현·push·배포 승인 자체가 아니다.

## 정식 결과 검수 — Astra

개별 편집·커밋이 아닌 고정된 작업 묶음을 검수한다. Jev의 1차 결과는 조사 입력이지 최종 판정이 아니다. 묶음의 전체 요구사항·누적 diff·직접 소비자·제외 범위를 확인하고, 실제 로그·산출물·검사 대상·선택 건수·필수 누락·잘못된 판정을 대조한다. 고위험·이상 징후·핵심 표본만 필요한 독립 재현을 추가한다. 구현자의 전체 검사를 복제하지 않는다.

제품 원본은 수정하지 않는다. 임시 빌드·테스트·캡처·보고를 지정된 검수 worktree에서만 만들고, 제품 소스·테스트·설정·커밋·배포는 변경하지 않는다. 검수 산출물 위치를 제품 미추적 파일과 구분한다. 정해진 결과 JSON에는 후보·검수 키·근거·차단/비차단·필수 미확인을 적는다. 소스·검사 입력이 바뀌면 영향을 받은 판정만 무효화한다. Claude의 감사·최종 판정은 대신하지 않는다.

## 최종 반영·보고

임시 통합 후보를 만든 뒤 유효한 묶음 검수·최종 필수 검사·Claude 감사 판정·실행 권한을 갖추어 지정 통합 담당이 반영한다. 각 브랜치 통과를 통합 후보 통과로 바꾸지 않는다. 배포는 `docs/PUBLISHING_CHECKLIST.md`의 적용 범위와 공통 배포 조건를 따른다. 커밋·원격 반영·실제 배포를 구분한다. 관련 변경·재사용·이월·미확인·결함·후속을 간결히 보고한다. 서명은 Claude·Codx·Muse·Gemini다.
