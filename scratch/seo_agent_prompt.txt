You are an expert Korean SEO copywriter. I have a JSON file `scratch/parsed_seo.json` containing 44 tools/paths.
Your task is to read `scratch/parsed_seo.json` and generate a complete JSON mapping for all 44 paths, adding missing fields based on the user's instructions.

The user's instructions are:
1. `menu` (메뉴명): 짧고 일관되게 유지합니다. (e.g. "엑셀 파일 합치기" -> "엑셀 병합")
2. `title` (title 태그): 검색 결과에서 어떤 작업인지 알 수 있게 씁니다. (Use the `title` provided in parsed_seo.json exactly).
3. `h1` (화면 제목): 방문한 사용자가 도구의 목적을 바로 이해할 수 있게 씁니다.
4. `description` (소개문): 무엇을 넣고 어떤 결과를 받는지 설명합니다. (1-2 sentences).
5. `h2` (소제목): 사용자의 실제 질문과 옵션 선택 기준을 담습니다. (Use the `h2` provided in parsed_seo.json exactly).

Example output format for "excel-merger":
"excel-merger": {
  "menu": "엑셀 병합",
  "title": "엑셀 파일 합치기·시트 병합 | Worklazy Tools",
  "h1": "엑셀 파일·시트 합치기",
  "description": "여러 엑셀·CSV 파일을 하나의 XLSX로 합칩니다. 시트별로 모으거나 데이터를 세로·가로로 이어 붙일 수 있습니다.",
  "h2": "여러 시트를 한 파일에 모으는 것과 한 시트로 이어 붙이는 것은 어떻게 다른가요?"
}

Please generate the JSON for ALL 44 paths found in `scratch/parsed_seo.json`. Ensure the JSON is valid and well-formatted.
Save the result to `/home/better0101/projects/worklazytools/scratch/full_seo_ko.json` using the `write_to_file` tool.
