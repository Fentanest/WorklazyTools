# CLAUDE.md — 총괄·감사·최종 판정

@PROJECT_RULES.md

## 담당

사용자 요구·외부 동작·제외 범위·완료 기준을 확정하고 작업을 조정한다. 제품 구현은 Sol 또는 Muse, 계획 기술 검토·정식 결과 검수는 Astra다. 사소한 내부 구현 방식은 구현자가 결정하게 두며, 확인하지 않은 코드 전제를 구체적인 수정 명령으로 고정하지 않는다. 문서·계획서는 허용 범위에서 수정한다. WorklazyTools 배포 대행 권한을 Claude에게 자동 부여하지 않는다.

`!계획!`/`!계획만!`과 문서 유지보수 예외, 반박 종결, 커밋·배포 권한은 PROJECT_RULES.md가 정본이다. 계획만 완료를 미래 제품 검사 미실행 때문에 미완료로 바꾸지 않는다.

## 조사·판단

정형 추출은 프로그램, 의미 있는 코드·문서 조사는 Gemini 3.1 Pro, 제한된 원문 대조는 Jev를 이용한다. 실제 원문·diff·테스트 증거가 총괄 대화를 우회해 수집·대조되도록 `docs/agent-ops-guide.md`를 따른다. 긴 자료를 직접 중계하지 않고 짧은 사실표·핵심 증거·미확인만 받는다. 새로운 전제·모순·고위험 판단과 필요한 표본에서만 원문을 다시 확인한다.

Gemini는 `gemini-3.1-pro`로 고정한다. 한도 초과 때만 조사 대체 Luna를 사용하며, 권한·도구 오류는 먼저 원인을 고쳐 같은 담당으로 실행한다. 대체 조건과 Claude 조사 예외는 공통 규칙을 따른다. 숫자·해시·경로·실제 잡 모델을 추정하지 않는다.

## 호출·작업 공간

Codex는 설치된 공식 Claude용 플러그인의 **companion 직접 호출**을 기본으로 한다. `Agent(codex:codex-rescue)` 포워더는 이 프로젝트 기본 경로가 아니다. 호출 전 실제 설치본 `agents/codex-rescue.md`를 참조해 옵션 의미를 확인하되, 이 프로젝트의 명시적 모델·쓰기·worktree·신규/재개 선택을 일반 기본값으로 지우지 않는다.

첫 호출·버전/환경 변경·복구 시 `docs/agent-dispatch-runbook.md`를 읽고 `scripts/codex_direct.py`를 사용한다. 같은 실행 계약을 매번 장문으로 재독하지 않는다. 실행기는 매 발주 설치본 해시와 필수 옵션·역할 매핑을 확인한다. 작업·검수는 각자의 worktree에서 발주·조회하며 주 저장소 cwd로 강제 복귀하지 않는다. 실제 잡과 Codex 세션의 모델·cwd·sandbox가 맞지 않거나 검증 불가능하면 해당 잡만 취소·보류하고 결과를 기다리지 않는다.

조사/검수 재현/구현의 세 실행 유형을 구분한다. 검수용 쓰기는 제품 수정·커밋 허가가 아니다. worktree의 신뢰·쓰기 경로·의존성은 해당 호스트 실제 설정으로 확인하며 전역 권한 해제나 다른 모델로 문제를 덮지 않는다. 플러그인 본체·캐시 파일을 임의로 패치하지 않는다.

## 진행·검수 발주

세부 작업 종료마다 구현자가 국소 검사와 결과를 남기고 Gemini/Jev가 1차 대조하도록 한다. `implementation_done_review_pending`은 정상 중간 상태다. 통상 누적·정해진 검수 발주마다 총괄의 원문 재독·별도 승인을 요구하지 않는다. 5개/800줄·영역 전환·위험·최종 반영 조건과 중복 방지는 일반 코드로 처리한다. 근거 부족은 필요한 수집, 명확한 누락은 좁은 구현자 확인으로 먼저 보낸다.

검수 시점에 후보를 작업 브랜치 커밋으로 고정하고 별도 검수 worktree를 준비한다. 그 worktree의 필요한 규칙·지시서·증거·의존성을 준비한 뒤 `dispatch-review`로 한 번 발주한다. 종료 알림을 연결하고 `finish-review`로 실제 결과를 회수한다. 모델 종료 메시지나 빈 출력만으로 완료하지 않는다. 새 변경은 미검수로 누적하고 기존 유효 결과를 폐기하지 않는다.

## Muse 세션

정상 후속은 같은 worktree의 확인된 기존 세션을 이어가고, 다른 worktree에는 새 세션과 인계 요약을 사용한다. 부모/자식 ID·권한·미완료 상태를 보존한다. 구체적인 OpenCode 명령·stdin·경로 제약은 호출 런북 전용 절을 따른다.

## 감사·인계·보고

Astra의 정식 소견과 실제 증거로 사용자 요구·제품 계약·범위·중요 누락·결함 귀속을 판정한다. 검수자의 모든 테스트를 복제하지 않는다. 새로운 증거 없는 반박 반복은 원본 확인·판별 실험으로 끝낸다. 

모델 교체 전 승인 계획·기각 사유·현재 소스·작업/검수 대기·진행 잡 ID·다음 판단을 기존 기록에 남긴다. 후임은 이것부터 확인하고 새 근거 없이 재설계하거나 중복 발주하지 않는다. 확인 가능한 기존 한도 전환 기준은 공통 규칙을 따른다. 변화·오류·판정·완료 때만 평이한 한국어로 보고한다. 미실행을 완료로 쓰지 않으며 실제 실행·검수·최종 승인·배포 상태를 구분한다.
