# U8-1 core handoff — Codx, 2026-09-13

Core implementation and scoped verification complete, pending independent Astra acceptance. No route/UI/SEO registration, product commit, push or deployment. Sole product writer stops at this freeze. Source `/tmp/worklazy-u8-impl`, base U7 `1df3c2e50edeb010272d82f15f279c1355f9f8ab`; U8-0 accepted separately, its frozen directory untouched.

## Change and ownership

Seven new files in `src/features/pdf-compare`: types, pure kernel, owned runtime, engine, session, report and pixel/text diff worker. Shared helpers and package/lock/vendor files are unchanged. API/lifetime details and exact integration ownership: `API-HANDOFF.md`; product/test allowlist and SHA: `evidence/source-freeze.json`. Exclude `API-HANDOFF.md`, this REPORT, `evidence/` and the `node_modules` symlink from product staging. Root owns canonical execution records; Sol owns subsequent UI writing/assembly and eventual final integration/publication.

- Physical index mapping preserves positions and unpaired tails. Manual mapping checks integer bounds, per-side 0–1 consumption and neither-side-empty rows. No alignment/model comparison APIs reused.
- PDF sniff plus current owned PDF.js helper; no pdf-lib admission. Page-at-a-time raw-order str+hasEOL LF extraction. Availability independent by side; unavailable never becomes equal/add/delete. Persistent results have strings, spans and compact summaries only. No uploaded-document accuracy field.
- Paired viewport at 96dpi, source geometry, common downscale to max4096 each side/area, opaque white and RGB >0/16/32 comparisons. Physical size change retained even at pixel0; unpaired ratios null. Sequential pixel/text worker; existing diffText only. Canvas arrays released before next pair; selected previews reload only selected pages and retain minimal items until release.
- During pending load/text extraction, abort destroys owning loading tasks. During active rendering, destruction is deferred until render cancel→settle→page cleanup. Worker cancellation terminates synchronous diff. Late results cannot register after abort. New runs invalidate prior output, serialize past cleanup and return null for superseded callers. Explicit cancel returns completed partial pages while active page canceled / remaining unknown.
- Pair isolation, localized behavioral report fields, C4 XLSX safety, lazy C2/C3 ZIP for >=2 reports. Unchanged or failed-with-no-pages report remains a nonempty summary. Original/render images are not exported. Reports enforce limitation warnings in both languages; stage2 must show canonical UI wording continuously.

## New execution / actual evidence

Environment: Node22.17.1, Chromium153.0.8010.12, Playwright1.63.0, PDF.js6.2.108, pdf-lib1.17.1, fontkit1.1.1, Poppler24.02.0. Existing U7 dependency tree is linked read-only for resolution; Vite cache/build writes stay inside this owned worktree's evidence directory. No install or dependency patch applied. Local harness loads no app route, ads, analytics or user documents.

| Command / scope | Result and evidence |
|---|---|
| `node tests/helpers/pdf-compare-fixtures.mjs /tmp/worklazy-u8-impl/evidence/corpus` | exit0, `fixtures-02.log`. Synthetic known-source normal ToUnicode, original production-shape historical mismatch, blank/image/size/multi controls. Test scripts bootstrap these if absent. |
| `node --test --experimental-strip-types tests/unit/pdf-compare.test.ts` | exit0, **5/5**, `unit-final.log`: mapping/blank position, raw text/EOL, common scale/size, threshold+opacity, unknown defaults. |
| `./node_modules/.bin/tsc -p evidence/tsconfig-core.json` | exit0, `tsc-core-final.log`; scoped app compiler settings/transitive imports, not whole app tsc. |
| `node tests/pdf-compare-golden.mjs` | exit0, `golden-oracle-final-02.log`, `golden-results.json`: **16 engine scenarios / 22 pages**, exact normal3 and historical wrong3 strings, same input pixel0, word/number/Korean/color/image changes, blank/image unavailable, white size-only diff0+sizeChanged, large common scale, unpaired/manual mapping; selected canvases match independent PDF.js render hashes for the exact left/right input. |
| `node tests/pdf-compare-smoke.mjs` | exit0, `smoke-bootstrap-final.log`, `smoke-results.json`: actual delayed read and text abort, render and pixel-worker abort, reply-before-registration abort, pair isolation, superseded null, selected physical pages/release, invalidation/disposal; completed/canceled/unknown partial states; actual XLSX3 + ZIP2 XLSX reopened. |
| `node tests/pdf-compare-negative.mjs` | exit0, `negative-01.log`, `negative/results.json`: **4 actual product mutants rejected** (drop physical page, one-side scale, unknown→complete, worker deleted-number span removed). Original worker retains deleted123. |
| `PDF_COMPARE_MUTATION=late-registration PDF_COMPARE_EVIDENCE=/tmp/worklazy-u8-impl/evidence/negative-late node tests/pdf-compare-smoke.mjs` | **expected exit1**, `negative-late-01.log`: removing actual engine's post-yield abort check registers complete instead of canceled; assertion rejects it. Isolated Vite transform only, product file unchanged. |
| `node evidence/build-core.mjs` | exit0, `build-core-final.log`, core-dist; scoped production harness+worker build only. Large-chunk warning retained; no threshold increase/diet. One existing PDF.js display mjs and one PDF.js worker mjs; shared helper's existing thumbnail worker is also emitted alongside new pixel worker. Full route/app graph metrics deferred. |
| `python3 evidence/poppler-current.py` | exit0, `poppler-current-final.log` and per-command logs: current exact synthetic corpus **12/12 same-input pixel0, 5/5 mutation nonzero**. Cross-renderer pixel equality is not an oracle. |

Real render cancellation call sequence in `smoke-results.json`: **render, cancel, settled, cleanup, cleanup, destroy, destroy**. Selected-page test uses before page2(Korean) / after page0(ASCII); canvas hash oracle additionally checks each side's exact independent source rendering, not merely differing hashes. No URI cache or raw persistent PDF.js object is introduced.

Report reopen checks partial workbook 3 data rows Complete/Canceled/Unknown with partial=true; no-page failed workbook one summary row; unchanged workbook one row; suspicious filename `=1+1.pdf` remains a literal string; two ZIP entries reopen as valid nonempty workbooks. The acceptance-example sentinel from U8-0 is **not** counted as product mutation proof.

## Reuse, failures and limits

U8-0 accuracy/visual disposition is reused as prior feasibility, not current new-kernel verification. Current browser and Poppler observations above are newly executed on the current test corpus. Normal fixtures author their own correct ToUnicode solely as independent test controls; historical producer/ToUnicode defects remain unfixed. Historical extraction remains 0/3 source-exact, 3/3 expected incorrect observations. No document identity guarantee or automatic source accuracy detection is claimed.

Original failures retained: root dependency directory lacked U7 docxtemplater/pizzip during broad app tsc attempt (`tsc-01.log`); switched own symlink to existing U7 tree. New TextItem import/parameter-property test compatibility corrected locally. First unit couldn't strip parameter property (`unit-01.log`); normal explicit property passed. Vite watcher quota (`golden-01.log`) fixed with watch:null, no OS/global change. Early Vite all-entry alias scan warning (`golden-02.log`) fixed by scoped entry+alias; subsequent run clean. First image fixture was white, so blank/image difference wasn't asserted; replaced with black embedded pixel and explicit nonzero oracle, preserving earlier observations. Independent direct-render oracle initially omitted its own workerSrc (`golden-oracle-final.log`); fixed only harness URL setup and reran. No failed run is reported as pass.

`golden-04` overlapped the render-abort ordering refinement; marked nonfinal in VALIDITY-NOTE, replaced by final tests. Smoke's last successful core run predates only the independent golden-oracle URL exports added to the shared test HTML; those are plain URL constants and do not change smoke/product execution. Scoped build was on the same final product source before those test-only oracle imports. Source freeze distinguishes product and test hashes; no product code changed after final core checks.

**Not this stage:** route/UI/ko-en featureMessage/SEO/static/ad tests; new public registration; full build/unit/static/Word-HWP whole regression; 1/20/200p performance/heartbeat/total heap, DPR2/mobile/theme, rotated CropBox/UserUnit/encrypted corpus, all stage3 hostile mapping/report mutations and final bundle/network inventories. Existing source viewport behavior is used but those broad edge cases are not claimed tested. No unlimited-heap guarantee follows from canvas bounds. Future UI must clear its own references on invalidation and never register null/superseded results.

**Disposition:** no known new blocker from scoped checks. Independent Astra should review diff/allowlist/evidence match, then targeted owner cancellation, unavailable/partial and output compatibility samples; do not replicate the full scoped matrix without a new reason. Stage2 starts only after root accepts and transfers sole-writer ownership. No commit/push/deploy performed.
