# U8 final verification: verified reuse

This is a read-only evidence decision, not a final U8 PASS. No product or test was changed, and no build or test was run for this decision. The candidate under comparison is `/tmp/worklazy-u8-impl`; the final UI lifecycle repair still requires its own current-source run.

## Accepted reuse

| Check | Decision | Exact evidence and validity boundary |
|---|---|---|
| U8 PDF compare core stage 1 and stage 3 | **Accepted reuse for the exact seven core files** | `/tmp/worklazy-u8-stage3-core/source-freeze-end.json` records `coreUnchangedStartToEnd: true` and the seven file SHA-256 values; `/tmp/worklazy-u8-stage3-core/ROOT-ACCEPTANCE.json`, `HANDOFF.md`, and the stage-1 `/tmp/worklazy-u8-core-review/ROOT-ACCEPTANCE.json` record the accepted functional scope. Root separately confirmed the current writer copy has the same seven hashes. Reuse includes stage-1 functional tests and stage-3 1/20/200-page, mixed, rotation/CropBox/UserUnit and same-environment repeat results. The experimental strict cross-DPR pixel assertion exited 1 and is not represented as a whole-run PASS; the accepted contract is geometry/scale behavior plus the documented device/font rendering limit. If any of the seven hashes changes, this acceptance is invalid. |
| `npm run test:document-diff` | **Accepted reuse** | The actual exit-0 execution is `/tmp/worklazy-dc-deploy/logs/test-document-diff.log`: Node 22 strip-types, Pyodide 0.29.4, 5 fixture pairs, 62 sidecars and all declared negative/exception checks. Direct byte comparison of `/tmp/worklazy-dc-impl` to the current candidate found all 33 relevant product/test/fixture files identical: `src/features/document-compare/**`, `tests/document-diff-equivalence.mjs`, and `tests/fixtures/document-compare/**`. The Pyodide lock entry remains exactly 0.29.4 with the same integrity. The lockfile difference is the later additive document-generation dependency set and `@xmldom/xmldom` 0.9.11→0.9.12; this test imports Pyodide and the unchanged comparison/Python sources and does not import xmldom. U8's new PDF text comparison caller remains covered by the new U8 target rather than this reuse. |
| `TEST_SCOPE=word npm run test:browser` | **Accepted reuse for the Word scope** | `/tmp/worklazy-u6-final-word-diagnostic/REPORT.md` and `current-original.log` record the exact scoped command against the final U6 production dist with exit 0. Its start/end SHA map is identical and the six served Word worker/Pyodide assets are listed in `assets.json`; the earlier broad transient remains unresolved and is not called a broad browser PASS. Direct comparison from the tested `/tmp/worklazy-u6-impl` to the current candidate found all nine Word product/test files identical (`src/features/word-compare/**`, `tests/browser-smoke.mjs`). Pyodide 0.29.4, JSZip 3.10.1, Puppeteer 25.6.0 and their lock integrities are identical. The later lock additions do not change these packages. Existing Word route behavior is unchanged; final production build must still succeed, but a sibling lazy U8 route does not invalidate the completed Word behavior run. |
| `TEST_ONLY_HWP=1 npm run test:new-tools` | **Accepted reuse for the HWP slice** | `/tmp/worklazy-u6-final/logs/new-tools-attempt2.log` is an actual successful final-dist run and includes HWP editor/comparison; the dedicated test's `onlyHwp` branch is explicit in the unchanged `tests/new-tools-smoke.mjs`. Direct comparison from the tested U6 writer tree found `src/features/hwp-editor/HwpEditorPage.tsx` and `tests/new-tools-smoke.mjs` byte-identical. `@rhwp/core` and `@rhwp/editor` remain 0.8.6 with the same lock integrity, and JSZip/Puppeteer entries are identical. Reuse is limited to HWP; the combined log's image/audio/video results are not promoted into U8 requirements. If the final owner prebuild changes the fixed rhwp assets, rerun this named HWP scope. |
| `TEST_SCOPE=pdf npm run test:browser` | **Accepted reuse for existing PDF edit/range/convert** | `/tmp/worklazy-u6-final/logs/browser-pdf-scope.log` records the scoped command and successful PDF edit/range split/conversion completion. Direct comparison from the tested U6 writer tree found all 36 relevant files identical: `src/features/pdf-editor/**`, `src/utils/pdfOwnedDocument.ts`, and `tests/browser-smoke.mjs`. PDF-lib 1.17.1, pdfjs-dist 6.2.108, JSZip 3.10.1 and Puppeteer 25.6.0 retain the same lock integrity. U8 adds a sibling route and separate compare worker without changing these sources. Any final edit to the shared owned-document helper, existing PDF editor tree, browser test, or those dependency entries invalidates reuse. |

## Named rerun required

| Check | Decision | Why reuse is not proven |
|---|---|---|
| `PDF_FINISH_ORACLE_OUTPUT=<fresh path> PDF_FINISH_ORACLE_PORT=<free 4270-4279> npm run test:pdf-finish-oracle` | **Rerun on the final candidate** | The last recovered actual oracle log, `/tmp/worklazy-u4-mergegate2/logs/28-pdf-finish-oracle.log`, reports exit 0-equivalent output for preflight 87, allowed 56, excluded 31, both-renderer SHA 56 and negative controls under Node 22.17.1, Chrome 152.0.7977.64, Poppler 24.02.0 and pdfjs 6.2.108. `/tmp/worklazy-u4-audit3/oracle-fixture-crosscheck.json` later confirmed 157 fixture hashes and 44 legacy bytes against then-current source, and a fresh read-only comparison confirms all 157 recorded fixture hashes still match. The final U4 product freeze matches current `finish/structure.ts`, `finish/preflight.ts`, and 33/36 selected PDF-finish paths, but it does **not include hashes for `tests/pdf-finish-oracle.mjs` or `tests/helpers/pdf-finish-ocg-transform.mjs`**. The raw mergegate log also lacks a source/build manifest, as `/tmp/worklazy-u4-audit3/REPORT.md` explicitly states. Current Chrome is 153.0.8010.36 rather than the logged 152 build. Therefore source-run and browser-environment identity cannot be proven, even though the fixture and production imports match. A single named oracle rerun is sufficient; the broader U4 finish/browser/golden families need not be repeated. |

## Final-source checks that remain new

The final production build, full unit suite, static validation, U8 route/registration/SEO inventory, utilities, recovery inventory, PDF-compare bundle/request attribution, and the repaired U8 UI lifecycle/export reproduction must run on the final candidate. The U8 core seven-file tests are removed from the new-run list while their hashes remain unchanged; only the UI lifecycle layer needs a fresh U8 functional run after Astra's repair.

No source comparison connects U8 to unrelated Office or video engines, so those families are outside this rerun list. This decision must be rechecked only if the final repair reaches shared helpers, core seven files, dependencies, Vite/build configuration, or one of the accepted consumer paths.
