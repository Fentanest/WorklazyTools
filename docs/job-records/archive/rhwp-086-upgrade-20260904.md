# 작업지시서 — RHWP 0.8.4 → 0.8.6 업그레이드 (2026-09-04)

**상태: 정본 (2026-09-04 정본화 — 3차 왕복에서 Codex "이견 0"·[정본화 가능] 선언. "v2 확정 사항"(v3 정정 포함)이 우선 계약.)**
기준 해시: **`311c59e310734e9206629b05752f4228e842dbf0`**(main — 라이브 복구 직후). 근거: 사용자 지시(2026-09-04).

## 현행 실측

- worklazytools: `@rhwp/core`·`@rhwp/editor` **0.8.4** 고정 · 벤더 스냅샷 `public/vendor/rhwp-studio/0.8.4/`(**78파일·53.60MiB**) · prebuild 게이트 `validate-rhwp-vendor.mjs`.
- npm 배포: **0.8.5 없음, 0.8.4 → 0.8.6**(core·editor 모두 0.8.6 존재).
- 로컬 rhwp 레포(`/home/better0101/projects/rhwp`): main이 **v0.7.7**(033617e), **`v0.8.4`·`v0.8.6` 태그 모두 부재**(fetch 필요). 자체 `CLAUDE.md`·`CONTRIBUTING.md` 보유.

## v2 확정 사항 (1차 왕복 8건 — 우선 계약)

1. **벤더링 소스 확정**: `vendor-rhwp-studio.mjs`는 **GitHub `v<version>` 태그를 sparse clone**해 Studio를 로컬 Vite 빌드하고, core WASM은 worklazytools `node_modules/@rhwp/core`에서 주입한다(스크립트에 0.8.4 하드코딩 없음 — 파라미터화 확인). 따라서 **S1의 로컬 레포 갱신은 "변경 조사 전용"**으로 확정(벤더링 입력 아님). 로컬 소스를 쓰려면 `RHWP_SOURCE_DIR` + **HEAD가 `v0.8.6` 태그 커밋과 정확히 일치**해야 함을 명시. **PWA 제거 보강**: 현행은 `workbox-dcde9eb3.js` 파일명을 고정 제거 — 0.8.6에서 해시가 바뀌므로 **`workbox-*.js`·`registerSW.js`·`sw.js`·`manifest.webmanifest` 재귀 잔존 검사**로 교체. 완료 게이트: `npm run vendor:rhwp` 성공 출력·생성 manifest의 `tag`/`commit` 기록·PWA 잔존 0건.
2. **버전 치환 범위 전수(무차별 치환 금지)** — 대상: `package.json:34,35` · `package-lock.json`(RHWP 항목 `14,15,1379,1380,1388,1389`) · `src/config/rhwp.ts:1` · `src/pages/LicensesPage.tsx:8` · `tests/new-tools-smoke.mjs:113,167,190` · `THIRD_PARTY_NOTICES.md:18` · `public/legal/third-party-licenses.txt`(생성물 — 재생성) · 벤더 스냅샷 일체(생성물). **제외 명시: `package-lock.json:4644,4645`의 `modern-tar 0.8.4`**(무관 패키지 — 변경 금지).
3. **manifest·validator 강화(선행 작업)**: 현행 manifest는 파일별 SHA-256만 기록하고 validator는 **manifest에 열거된 항목만** 검사 — 예상 못 한 추가 파일과 크기 변조를 못 잡는다. **manifest를 `{bytes, sha256}` 구조로 확장**하고 **validator가 실제 파일 집합 ↔ manifest 집합의 완전 일치(추가·누락 모두 실패)와 bytes 일치까지 검사**하도록 보강. **`docs/OFFICE_EDITOR_ASSETS.md`에 RHWP 절 신설**(현재 RHWP 언급 0건 — version/tag/commit·패키지 버전·총 파일 수·총 bytes·manifest 자체 bytes/SHA-256·생성·검증·rollback 절차).
4. **구 0.8.4 스냅샷 제거는 스크립트로**(수기 삭제 금지 — 「생성물 직접 수정 금지」): 0.8.6 검증·왕복 스모크·시각 검수 **전부 통과 후**, 벤더링/정리 스크립트가 **명시된 이전 버전만** 제거. 최종 게이트에 **`rhwp-studio/0.8.4` 참조 grep 0건**(구 스냅샷 자체 제외) 포함. 참조자 실측: `tests/new-tools-smoke.mjs:167`(경로 하드코딩) — 갱신 대상.
5. **HWP 스모크 보강(업그레이드 회귀 게이트)**: 현행 HWP 전용 실행은 로드·레이아웃·버전 표기까지만 검사하고 **편집·저장·재개방이 없다**. 또한 `TEST_ONLY_HWP` filter가 **fixture 생성 이후**에 적용돼 전용 실행도 ffmpeg·3.8GiB sparse 미디어 fixture에 의존한다. 수정: ① **filter를 fixture 생성 전으로 이동**(HWP 전용 실행이 미디어 fixture를 만들지 않도록) ② 독립 고정 HWP/HWPX fixture 사용 ③ **왕복 게이트 신설** — iframe에서 sentinel 텍스트 편집 → host `HWP 저장`(`HwpEditorPage.tsx:103-139` `exportHwpVerify()`→`exportHwp()`) → 다운로드 Blob 캡처 → `@rhwp/core`로 sentinel·페이지 구조 확인 → **저장 결과를 Studio에 재개방**.
6. **라이선스 갱신 정정(v3 소유권 확정)**: 실행 명령은 **`node scripts/generate-third-party-licenses.mjs`**(초안의 npm 별칭은 부재 — prebuild에서만 호출). 생성기 실제 산출물은 **`public/legal/third-party-licenses.txt` + `public/legal/worklazy-license.txt` 2종**(v3 실측 정정). upstream `THIRD_PARTY_LICENSES.md`는 벤더 스크립트가 복사. **`src/pages/LicensesPage.tsx:8`은 소스 수정**. **root `THIRD_PARTY_NOTICES.md`는 「생성물 직접 수정 금지」 대상이므로 손으로 고치지 않는다 → 결정: 생성기를 확장해 root NOTICES도 생성기가 쓰도록 소유권을 일원화**(생성기 확장이 이번 단위 작업 범위에 포함). **`scripts/validate-static-output.mjs:254-256`**은 `@rhwp/core` 문자열 존재만 검사 → **core·editor 기대 버전 문자열 검사**로 보강. **`scripts/validate-static-output.mjs:254-256`은 `@rhwp/core` 문자열 존재만 검사** → **core·editor의 기대 버전 문자열까지 검사**하도록 보강(구 버전 잔존 통과 차단).
7. **시각 검수 계약 정정(v3 게이트 확정)**: `test:visual`은 현재 home·tools·excel-compare 3 route만 — **HWP 화면을 검사하지 않는다**(초안 오기 정정). HWP 자동 기준선 추가는 이번 단위 범위 밖(문서 로드된 iframe 상태 설계 필요) — **수동(Gemini 브라우저) 검수가 게이트**. **localQa 반입 방식 확정**: `ui-migration`의 도입 커밋 `0ba96ed`는 33파일을 건드리므로 cherry-pick 금지 → **`rhwp-0.8.6`에 최소 동등 구현을 별도 단일 커밋으로 추가**(`VITE_LOCAL_QA` 플래그 + 두 로더 차단만 — UI 마이그레이션 코드 미포함), `ui-migration` 병합 시 중복 해소는 후행 통합 절차(확정 8항)에 귀속. **검수 실행 게이트(명령 고정)**: `VITE_LOCAL_QA=1 npm run build` → `npx vite preview --port <포트>` 기동 → Gemini 브라우저 검수(HWP 편집 화면 로드·편집·저장 흐름) → **네트워크 로그로 GA·네이버·AdSense 요청 0 확인** → preview 종료. **최종 판정은 Gemini 단독이 아니라 Claude·Codex 실측 교차**(공통 규칙 「배포 전 로컬 시각 검수」).
8. **브랜치 전략·후행 통합**: `rhwp-0.8.6`(main `311c59e`에서 분기) → 검수 통과 → main 병합·배포 **유지**. 실제 충돌 표면은 벤더 디렉터리가 아니라(그쪽은 겹침 0건 실측) **`package.json`·`package-lock.json`·`docs/OFFICE_EDITOR_ASSETS.md`·`public/legal/third-party-licenses.txt`·`tests/new-tools-smoke.mjs`·`CHANGELOG.md`·`docs/review-notes.md`·`scripts/generate-third-party-licenses.mjs`·`scripts/validate-static-output.mjs`**. **후행 통합 절차(ui-migration 책임)**: main 재반영 후 **결합된 `package.json`으로 lock 재생성**, UI 브랜치의 QR 라이선스 생성 로직을 보존한 채 `public/legal` 재생성, HWP 스모크는 UI selector 변경과 0.8.6 왕복 검사를 **양쪽 모두 유지**, 자산 문서·CHANGELOG·review-notes는 양쪽 기록 병합.

## S2 조사 결과 (2026-09-04 — Claude 로컬 태그 실측 + Codex 대행. Gemini 한도 초과로 「부하 분산과 대체 경로」 적용)

- **원격 태그 실재 확인**: `v0.8.6` = `f1f9c6ae58344ee9368996d3543f76b9345cf227`(peeled), `v0.8.4` = `496333b2…`. 로컬에 두 태그 얕은 fetch 완료.
- **릴리스 성격**: v0.8.4 이후 **2,214커밋·262 PR 누적 PATCH**(2026-09-02). CHANGELOG 명시 — **"기존 JSON 봉투 major는 유지"**(breaking change 없음). 규모상 Studio 산출물 구성 변화 가능성이 크므로 **해시표 전면 갱신 전제**.
- **통합 영향 후보**: ① Studio 편집 기능 대폭 추가(HTML·Word 내보내기·차트 데이터 편집 UI·IME `Ctrl+A`·머리말/꼬리말 API·표 리사이즈·캐럿/눈금자) — HWP 편집 화면 UI 변화 예상(시각 검수 대상) ② 저장·왕복 보존 다수 수정(개체·표·그림·HWP3·HWPML 2.1) ③ 신규 API `setExactFontInstance`/`clearExactFontInstance`(추가형) ④ 보안: HWPX 컨테이너·파서 재귀 깊이 상한 ⑤ `.xlsx`가 HWP viewer로 잘못 열리던 경로 차단(#6547).
- **★ 신규 빌드 플래그 `RHWP_WITHOUT_HWPCTRL=1`**(`rhwp-studio/vite.config.ts` 실측): 설정 시 `npm/hwpctrl-ocx` 플러그인이 **빌드 시점에 tree-shake되어 `studio-plugin` 청크 자체가 산출물에 남지 않음**. 주석에 "studio만 떼어 배포할 때 쓴다"고 명시 — **우리 벤더링이 정확히 그 용례**. **S3에서 채택 여부를 실측 판단**(채택 시 벤더 스냅샷 축소·불필요 코드 제거 / 미채택 시 사유 기록). 기존 `RHWP_DISABLE_EXTERNAL_WEBFONTS=1`은 유지.
- **미확인(S3 실측 항목)**: PWA 산출물 파일명(`workbox-*.js` 해시) 변화 · WASM 파일명·바이트 · 폰트/자산 파일 수·구성 · 실제 CSP meta 내용 · 우리 통합 코드(`hwp-editor`·`config/rhwp.ts`)의 API 호환.

## 단계

- **S1(Gemini — 변경 조사 전용)**: 로컬 rhwp 레포 자체 규칙(`CLAUDE.md`·`CONTRIBUTING.md`) 선독 → `git fetch --all --tags` → main 갱신·**`v0.8.6` 태그 존재 확인**. **로컬 미푸시 커밋·변경이 있으면 덮어쓰지 말고 중단·보고.** 산출: 전후 HEAD·태그, v0.7.7→v0.8.6 CHANGELOG 요약.
- **S2(Gemini)**: 0.8.4→0.8.6 변경 중 통합 영향 항목 추출(Studio index.html/CSP 구조·PWA 산출물 파일명·폰트/자산·에디터 API·초기화 옵션·breaking change). 「Gemini 산출물은 단서다」 — S3 실측으로 확정 후 반영.
- **S3(Codex — 반영)**: 확정 3항(manifest·validator 보강) 선행 → 확정 2항 범위 치환 → **(v3 추가) 설치 의존성 동기화 게이트**: `npm install`(또는 lock 갱신 후 `npm ci`)로 **설치된 `node_modules/@rhwp/core`·`@rhwp/editor`가 정확히 0.8.6임을 확인한 뒤에만** 벤더링 진행(현행 벤더 스크립트는 목표 버전과 설치 버전을 대조하지 않고 WASM을 복사 — 구 WASM 혼입 위험) → `npm run vendor:rhwp`로 0.8.6 스냅샷 생성 → 확정 6항 라이선스 생성기 확장·재생성·검증기 보강 → 확정 5항 HWP 스모크 보강.
- **S4(Codex — 검증)**: 아래 「검증」 절 전부 실행 + Gemini 시각 검수 통과.
- **S5(Codex — 정리·확정)**: **검증 통과 후에만** 구 `0.8.4` 스냅샷을 스크립트로 제거(확정 4항) → **최종 rebuild + validator + `rhwp-studio/0.8.4` 참조 grep 0건** 재확인 → 커밋·브랜치 push.

## 검증

prebuild 게이트(보강된 validator 포함) · `npm run build` · `test:unit` · **`TEST_ONLY_HWP=1 npm run test:new-tools`(왕복 게이트 포함)** · `test:office` · `test:static`(보강된 버전 검사) · `test:visual`(전역 비-HWP 회귀 — HWP 미포함 명시) · **Gemini 브라우저 시각 검수**(추적 차단 빌드) 차단 결함 0 · 기록 이원 체계.

## 명시 제외

rhwp 레포 자체 기능 수정·기여 · 0.8.6 신규 기능의 제품 노출 확대 · 벤더 자산 수기 편집 · `modern-tar` 등 무관 패키지 버전 변경 · HWP 자동 시각 기준선 신설(선택 — 별도 판단).

## 시각 검수 판정 (2026-09-04, Claude — Gemini 검수 + 라이브 대조 실측 교차)

Gemini가 차단 결함 5건을 제기했으나 **전부 RHWP 업그레이드와 무관한 기존 UI 상태로 확정**(RHWP는 HWP 편집기 iframe 내용만 교체하며 사이드바·하단 내비·언어 토글을 건드리지 않음). 판정 근거는 **라이브(worklazy.net = RHWP 미반영 main) 동일 화면 캡처와의 대조**:

1. "사이드바 하단 겹침·문구 잘림" → **오탐**. 실측: 사이드바 `scrollHeight == clientHeight`(866), 마지막 항목("문의·건의·버그 제보")이 뷰포트 내 표시(`lastVisible: true`). 도구 목록만 자체 스크롤되는 정상 구조.
2. "모바일 하단 로그 카드 파묻힘" → **오탐**(뷰포트 첫 화면 캡처 — 스크롤 가능 페이지의 정상 렌더. 브랜치 QA 1회차와 동일 유형의 오판).
3. "언어 토글 썸 상하 오정렬" → **RHWP 무관**(라이브 동일).
4. "하단 내비 활성 배경이 아이콘+텍스트를 덮음" → **RHWP 무관·기존 디자인**(라이브 동일).
5. "Excel Merger 카드 하단 파묻힘" → **오탐**(첫 화면 캡처, 2번과 동일).

**개선 권고 중 P2 편입 대상**(RHWP 배포와 분리): 다크 모드 녹색 안내 배지의 배경↔텍스트 대비 개선 · 하단 내비 활성 인디케이터 형태(알약형 검토) · 카테고리 필터와 안내 배지 사이 여백.

**결론: RHWP 0.8.6 배포 차단 사유 없음.** Codex 상호작용 검수(편집기 조작·저장 왕복·콘솔·추적 0) 결과와 합산해 병합·배포 진행.

## 반박 기록

### Codex 1차 (2026-09-04, 완료 — 8건 전원 수용 → v2 확정 사항. 실측 기여: vendor 스크립트 GitHub 태그 clone 구조·workbox 파일명 고정 위험, 0.8.4 하드코딩 전수(27행·modern-tar 오염), manifest 열거분만 검사·크기 미검증·문서에 RHWP 절 0건, 구 스냅샷 78파일 53.60MiB·참조자, HWP 스모크 커버리지 공백·fixture 순서 문제, 라이선스 스크립트 부재·소유 파일 분리·static validator 허술, visual matrix에 HWP 없음·localQa가 main에 부재, 브랜치 충돌 표면 9파일. 판정 "재왕복 필요")
### Codex 2차 (2026-09-04, 완료 — 8건 표제 반영 확인, 실질 공백 5건(설치 의존성 갱신 게이트 부재·라이선스 소유권 미결정 및 `worklazy-license.txt` 누락 실측·구 스냅샷 제거 순서 자기모순·localQa 반입 방식(0ba96ed 33파일) 미확정·검수 실행 명령/교차 판정 누락) → v3 정정)
### Codex 3차 (2026-09-04, 완료 — 5건 전건 해소 확인·신규 공백 없음, **"이견 0"·[정본화 가능] 선언** → 정본 확정. 착수: S1(Gemini 레포 갱신·조사) 즉시 → S2 → S3~S5).
