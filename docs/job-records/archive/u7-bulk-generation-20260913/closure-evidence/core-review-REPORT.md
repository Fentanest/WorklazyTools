# U7-1 independent core review

**No unresolved core-stage blocker found. The frozen U7-1 core is accepted within this review scope.** This is not U7-2/3 UI, registered-document privacy, final licensed build, deployment, or whole-device memory acceptance.

Reviewed base `5ac8b6473d1786691fff7a83c5fde126f16e6af1` plus exactly 19 frozen paths, including untracked new core/tests, aggregate `1e340e165d4cbf9b5c940ebaef5d0771794602a20e3601375d101082217f4f76`. A base git archive plus SHA-verified overlay lives in `source/`; installed dependencies were reused via symlink without installation or writes. All 19 original and copied paths still match. Installed Core3.69.3/PizZip3.2.0/xmldom0.9.12 match the fixed lock. `invariance.json` records source, artifact and root/worktree status checks. Status text happened to remain unchanged; core SHA checks are the invariant even when concurrent UI files later change. No product edit, commit or push.

## New execution

- `node --experimental-strip-types /tmp/worklazy-u7-core-review/source/independent.mjs > /tmp/worklazy-u7-core-review/independent.log 2>&1`: **exit0**, Node22.17.1. Actual C1 CSV parsing, exact physical row/value/leading-zero/empty expectations, actual scalar renderer and memory storage. Old three outputs → next attempt first row committed, second write fails → only the new committed row remains, later row not started. Prior storage survives until matching post-commit acknowledgement; stale acknowledgement does not retire it. A third attempt commits its first row, then cancellation during a delayed second-row producer keeps BUSY until settlement, removes the second row and marks remaining rows canceled. Disposal clears all stores. The bridge is in-process in this independent sample; native worker and real OPFS results are reused below.
- `python3 /tmp/worklazy-u7-core-review/oracle.py > /tmp/worklazy-u7-core-review/oracle.log 2>&1`: **exit0**. Two independently generated DOCX files (`old-middle.docx`, `new-first.docx`) reopen with ZIP CRC/XML parsing, exact body/table/header/footer values, literal braces unexpanded, and exact unmodified physical parts. Template and CSV hashes remain unchanged.
- Same oracle command with `swap`: **exit1**, `oracle-mutant.log`, rejecting the second row's output when substituted for the first expected artifact. Initial `oracle-first.py/log` **exit1** are preserved: exact ZIP-entry equality encountered only added zero-byte directory `word/`, the already documented Core container behavior. The final checker permits only that named empty directory and still requires every original entry and byte-identical unmodified file.
- From `source/`: `./node_modules/.bin/tsc -p tsconfig.app.json --noEmit --tsBuildInfoFile /tmp/worklazy-u7-core-review/app.tsbuildinfo > /tmp/worklazy-u7-core-review/types-app.log 2>&1`: **exit0**. Actual app config includes `src`, hence all frozen core TS, with no concurrent UI overlay. This closes a submitted-evidence gap: root tsconfig has `files: []` and references, so the author's reported plain `tsc --noEmit` is not accepted as proof of app checking. Earlier error log contains actual source diagnostics but no command provenance; the final empty log alone cannot establish its target. This new invocation supplies the missing evidence; it is not the author's execution.

## Reused and checked evidence

The canonical mapping/display-error precedence, explicit aliases, whole-run C2 registry and ownership errors, single-pass filenames, U7-0 custom-related-part and wrong-namespace repairs, own-data descriptors, sequential worker/stale reply handling, storage publication, C3 complete-only export and C4 text-safe report paths were reviewed against the fixed source and tests. No core C1/C2/C3/C4 shared implementation was changed. Dynamic ZIP/manifest imports remain execution-only; actual registered route attribution awaits integration.

Author evidence directory `/tmp/worklazy-u7-preflight/implementation`: final unit log has **11 executed / 11 passed / 0 skipped**, covering C1 real error/cache versus literal text, aliases, NFC/case/nonadjacent cross-file collision ownership, suffix255/256, two repaired template boundaries, all-error retry, worker stale replies and delayed real C3 close cancellation. Browser-final log/raw has **16 cases / 6 DOCX / workers11 terminated11 / external0 / errors0**, using preserved browser-third dist on Chrome153.0.8010.36. Harness source really calls OPFS write/read APIs, wraps actual writable.write for delayed settlement and injected write/quota faults, uses actual 200MiB Blob.size equality/+1, and cancels the native worker on rendering progress. These are not a disk-exhaustion or total-heap experiment.

The unchanged external-attempt gate's mutant log fails with `1 !== 0`; raw contains one blocked synthetic worker fetch. The Python oracle checks six per-source outputs and complete ZIP entries; actual control/drop/swap/truncate exits are 0/1/1/1. LibreOffice reopening PDF exists with its declared SHA. All **7 dist entries + 12 final evidence entries + 1 LO output** were byte/size/SHA verified against their submitted manifests. These full browser/unit/six-output/LO executions were inspected and reused, not duplicated. Author preserved initial TS/test/harness/vendor-notice failures; no zero-count result was promoted to PASS.

## Limits and final handoff

No new product defect was reproduced. The app-type command provenance gap is resolved by the independent actual app-project check; author was asked to correct the command claim without altering core.

The 200MiB policy covers actual registered Blob sizes in one memory run, including equality and refusing +1. It excludes pending render/write copies, old/new result overlap until acknowledgement, input/worker heap, ZIP and manifest. OPFS estimates remain advisory; write/close exceptions stop the run without mid-run fallback. No whole-heap/mobile safety claim follows.

UI must still obey reset-before-file-replacement, post-React-commit acknowledgement, URL lifetime, canceling/retry lock and old-result versus latest-attempt distinction. Actual registered privacy/controller coverage, ko/en UI/SEO/advertising/accessibility/visual work, route bundle measurements, final whole build/unit/static and the remaining canonical consumer checks are integration gates. Existing NanumGothic OFL vendor input preparation and actual notice generation remain unresolved final-integration work, not a successful generated-license result. No vendor reinstall was performed here.

This concludes the bounded U7-1 core review. Reuse is tied to these 19 hashes and their fixed shared dependencies/inputs; subsequent UI integration requires its own relevant consumer checks, without reopening unrelated U7-0 corpora or duplicating the author's full suites.
