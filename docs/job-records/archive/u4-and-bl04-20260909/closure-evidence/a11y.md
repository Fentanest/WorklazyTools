# Independent accessibility harness audit — Codex Astra

Verdict: PASS for other author's tests/accessibility-audit.mjs and tests/unit/accessibility-audit.test.ts changes. No new P2 gate defect found. This verdict excludes my own PDF thumbnail worker and PDF finish smoke-directory changes. No tracked writes, commit or push; all new evidence under this /tmp directory.

## Resolution scope and actual evidence

The new resolver is available only after mandatory measurements for the two explicitly enumerated inclusion spans and only for Axe color-contrast. It resolves the actual raw target via document.querySelector, then requires element.matches(exact measured span selector), not a broad ancestor. It assigns the existing f2-watermark/f3-stamp owner, retains original target and reasons in resolvedIncomplete, and records measurementKind/Target/Selector. Unmatched rules/targets follow existing owned/shared classification. The old stamp-notice title/body measurements remain mandatory; the adjusted stamp crosscheck delegates only the independently validated finish-inclusion kind.

Independent evidence.mjs read the preserved original integration/a11y.json and verified its SHA remains 9f020297f611a0c3f6c2b5f00c7b3dd26c91fec37bfb1844f3a1e010af79227b. All34 original F2/F3 raw targets were re-resolved on current QA4277: each has exactly one node and equals the corresponding inclusion-measurement DOM node. Their raw selectors vary by Axe CSS representation, so literal selector-string equality would have been incorrect; actual node identity is verified.

Root's final a11y-final.json has28 pages, violations0, owned incomplete0, pixel-resolved50, inherited787. Independently paired all34 original records to exactly one new finish-inclusion resolved record by page, rule, owner, exact JSON target and exact reasons. Original inherited787 is unchanged at787, so none of these34 was silently reassigned to inherited debt. Prior16 pixel resolutions plus new34 account for50.

## PNG and threshold audit

Independently opened all54 final inclusion PNG files, recomputed SHA256, verified every pixel's opaque alpha, checked sample count equals width×height, and recomputed minimum WCAG luminance contrast from recorded foreground and every PNG background pixel. All reported ratios agree within1e-12 and remain >=4.5. Focused6 PNGs were likewise independently verified. Thus sampled-image evidence supports the actual claims, rather than relying solely on ratio fields or a syntactically valid hash. The checker generates PNG bytes and SHA in the same measurement operation; the independent audit validates persisted files. Exact selectors, owner, one-target count, positive integer samples, digest format and ratio threshold remain fail-closed assertions. No contrast threshold or registered page list was lowered.

## Re-executed negatives and units

node --experimental-strip-types --test tests/unit/accessibility-audit.test.ts:18/18 PASS, including11 new malformed/missing/mismatched/low-ratio evidence variants. unit.log retains output.

Re-executed the supplied negative browser probe with only its output directory redirected here. Light/dark × watermark/stamp4 cases set foreground and background to#777 in memory; each measured1:1 and was rejected by the actual assertion. Clean controls passed. negative.json and four negative/control PNG pairs retained. No product stylesheet or tracked file changed.

The final28-page run was performed by root once; I independently validated its results and source binding instead of repeating unrelated pages. Root's fullunit506 and the prior same-round legacy44/diff0 are integration evidence, not extra suite executions claimed by this audit.

## Integrity

Start/end git status identical. All2726 tracked file SHA entries identical. Audited file hashes match the author's frozen checks.json:
- accessibility-audit.mjs:84a41e00922adbe51b216b96f3a940f21f102e653ba895dfd6fe96bb79c7c8bd
- accessibility-audit.test.ts:1e8184fbee269807a94bf1f78d66cbfd7fb8e6c1cf0988caa2bb315b5198098a

Files: evidence.mjs, evidence-final.log, evidence.json, final-evidence.json, negative.mjs/log/json, unit.log, status-start/end.txt, sha-start/end.txt and empty diff files. The inherited787 remains unresolved debt; it is not converted into a blanket accessibility-success claim.
