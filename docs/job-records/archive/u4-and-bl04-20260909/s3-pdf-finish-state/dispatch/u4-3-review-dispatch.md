# 지시서 — U4-3 구현 검수 (2026-09-07, Claude → Codex **gpt-6-astra**)

## 0. 선독
`PROJECT_RULES.md`(「현지화·SEO·AdSense 동시 검토」·「내부 구현 비노출」·「광역 금지 계약」·규칙 19) → `AGENTS.md`(검수: 지시서·정본 대비 누락·왜곡·검증 미실행을 **재현 명령**으로 판정) → 정본 `docs/jobs/todo/pdf-finish-20260905.md`(「정본화」 절 → route/preset/navigation 확정 13·14·15·23 + H1 · 토큰·선택 확정 7·9·24 + D2·N1 · clock 확정 10 + H3·D6 · 폰트 확정 1 + N3 + D8 · 여러 줄 확정 21 · D5 v6·v7(메인 스레드 엔진·`pdfFontEmbed` 공용 청크·협력적 취소) · 확정 26 · H2 + 5차 H2(b) · H5·H6(g) · 확정 3 · 「명시 제외」) → S2b 정본 `docs/jobs/todo/qr-font-20260906.md`(U4 전체 OTF 유지·QR selector 미전파) → sol 지시서 `<scratchpad>/u4-3-dispatch.md`(단계 경계: route 3·탭 2, 탭 바는 구현된 탭만) → sol 보고 `/tmp/worklazy-u4-3/REPORT.md`·logs·`shots/` (`<scratchpad>` = `/tmp/claude-1000/-home-better0101-projects-worklazytools/f5cf1bca-6bf0-442b-b9d4-e7cb45f9c96d/scratchpad`).

## 1. 성격·기준
- **검수 잡(쓰기 모드·저장소 불변)** — 추적 파일 수정·커밋·push·브랜치 전환·설치 금지. 산출물 `/tmp/worklazy-u4-3-review/`. 브랜치 `s3-pdf-finish` HEAD `c8bff1fd1ab64f89afb7240778e0a373c953d1a3`(기준 `446a1e3`), `main` `5bc6854175331bdd73b267784d9633cdccda8446`. 시작·종료 `git status`·추적 파일 SHA 불변 증명. 빌드·시각 회귀·브라우저 **직렬** `NODE_OPTIONS=--max-old-space-size=4096`. 검수 스크립트는 자신의 것.

## 2. 검수 항목(재현 필수)
1. **범위·불변**: `git diff --stat 446a1e3..HEAD` 목록 ↔ 지시서 A~F. 기존 4모드 UI/문구/동작 불변(organize·image-to-pdf·pdf-to-image·convert 패널 diff·locale 기존 키 값 diff 0, `TEST_SCOPE=pdf`) · legacy oracle diff 0 · 공용 lifecycle·Excel diff 0 · QR: `test:qr-bulk`·`test:qr-font-render` 통과 + `pdfFontEmbed` 이관으로 QR PDF byte/Poppler 동일 · `pdf.worker.ts` legacy 경로 무변경 · `expectedToolIds` 20.
2. **route/preset/navigation(확정 13·14·15·23)**: `PdfToolMode` 5값·`PdfFinishTab` 4값·preset 타입 문안 일치 · 3 route 가 같은 `PdfRoute mode="finish"` + typed preset(전역 generic router·페이지 복제 없음) · 탭 전환이 route 를 바꾸지 않음(history 길이) · navigation 5칸(`repeat(5,…)`·`grid-cols-5`, finish 가 organize 다음, 직접 진입 active 스크롤, `data-pdf-nav-mode`) · **`rg "pdf-tool-navigation a:nth-child"` 0건** · 320/390/820/821 × ko/en fade·active 가시성(실제 Chrome 측정).
3. **SEO/static/광고(규칙 4·5)**: `seo.ts` 3경로 ko/en 메타·FAQ · canonical 전부 `/finish` · 정적 페이지 3경로 ko/en 생성 + `validate-static-output` 허용목록 · 사이트맵·소셜 이미지 · `npm run test:static` · 광고 로더 정상·격리 0(광역 금지 검사 실행) · 원시 예외/내부 명칭 노출 0(finish 화면 문자열 전수 grep: `Worker`·`code:`·`Error:`·영문 예외 원문).
4. **S0 상속(H2(b))**: finish 패널 별도 lazy 청크, 바깥 `PdfRoute` Suspense/ToolReady 만 재사용, 내부 fallback `.tool-route-loading` + 내부 `ToolReady` — 청크 404 주입 시 reload ≤1·가드 정리 실측(3 route). recovery 스모크 20 표본 불변.
5. **장식 엔진(확정 1·9·10·21·24 + D2·N3·D8·D5)**: U4-1 모듈 **재사용**(재구현·복제 grep) · 순서 load → 토큰/전처리/coverage → 폰트 결정(Helvetica / 전체 OTF `QR_LABEL_FONT_PATH` size·SHA 검증 `subset:false`, 문서당 단일) → 페이지 루프(geometry 앵커·upright) → save → 등록(양보 후 abort 재검사) · 매 반복 `yieldToEventLoop`(Promise.resolve 금지 grep) · abort 검사 지점 6곳 · clock 1회 캡처·locale 전달 · `{date}` 로케일 기본/형식 지정 · D2 산식(startPage 4·startNumber 5·표지 제외·물리 4·6 → "5"·"7") · **출력 oracle**: U4-0 fixture(회전 4종·CropBox) 에 번호·머리말 적용 → Poppler+PDF.js 렌더/텍스트 추출로 위치·upright·글자 확인(`Русский` Noto·`Résumé €` Helvetica·누락 scalar 필드 오류) · 암호 fixture → "편집 불가" 안내 · 취소 → 기등록 결과만 보존.
6. **화면(확정 26·N1)**: 토큰 도움말 5종 · 6영역 · 범위 텍스트 ↔ 홀짝 ↔ 썸네일 토글 **양방향 동기**(N1 골든 `2-8+even` 3쪽 토글 → `{2,3,4,6,8}`·`2-4,6,8`·parity all) · 하한 미만 disabled · 빈 set 실행 비활성 · `PdfOrganizePanel`/`PdfThumbnail` 재사용(복제 grep) · 오버레이 미리보기 근사 + 한계 안내 ko/en · 취소 버튼 finish 전용.
7. **하네스(H5·H6(g)·D6·H3)**: `npm run test:pdf-finish` 존재·실행(12 진입·SPA 전환·preset initialTab·404 가드·실행/취소/재시도·필드 오류·geometry oracle·영어 모바일) · 시각 scenario 2탭 + nav 3상태·`mobile-320` viewport(기존 scenario 320 중복 없음) · `visual-config.test.ts`·`visual-clock.test.ts` 갱신 · `toolReasons` pdf-editor · **`LANG=ko_KR.UTF-8`/`en_US.UTF-8 npm run test:visual` 실행(baseline 대비 0 diff, 총 시간 ≤20분 기록)** · `rendering-baseline.mjs` finish 3 route 등록 + `npm run test:rendering` CLS ≤0.1(목표 0) · `accessibility-audit.mjs` finish desktop+412(+en 1) + `VITE_LOCAL_QA=1 npm run build` → `A11Y_MAX_TOTAL=0 npm run test:a11y`.
8. **번들(H2 ①·D5 확정 28)**: `npm run bundle:measure`(`/tmp/s3-bundle-baseline.json`) 5종 상한·override 없음 · finish 패널·`pdfFontEmbed`·fontkit 귀속(route/shared) 표 · QR route 의 fontkit 이동이 "이동"으로 분류되는지(순증분 게이트 정합).
9. **공통 검증**: tsc · unit · 직렬 build · static · `test:pdf-finish` · `TEST_SCOPE=pdf` · `test:browser` 전체 · `test:new-tools` · `test:utilities` · `test:office` · `test:qr-bulk` · `test:qr-font-render` · `test:recovery` · `fixtures:pdf-legacy-oracle` · `test:excel-cleaner`·`test:excel-compare` · `css:orphans` · `legacy:manifest` · registry 20 · `git diff --check`. production dist 대 main: PDF/finish/QR 관련·참조 전파 외 동일(정규화 비교).
10. **시각 육안 입력**: sol `shots/`(3 route × ko/en × desktop/mobile) 개수·해상도 대조; 자신도 `VITE_LOCAL_QA=1` 빌드 preview 에서 같은 12장 + 다크 모드 캡처를 `/tmp/worklazy-u4-3-review/shots/` 에 남기고 파손(정렬 붕괴·잘림·토글 썸 이탈·겹침) 여부를 DOM 실측으로 판정.
11. **범위 밖 발견 판정(sol 이 Claude 판정 대상으로 올림)**: 정본이 지정하지 않은 UI 초기값 — 기본 텍스트 템플릿·초기 6영역 위치·글자 크기 10pt/24pt·색상·미리보기 불투명도·출력 파일명 접미사 `-finished.pdf`. 각 항이 (a) 사용자에게 노출되는 계약인지(파일명·문구는 노출) (b) ko/en 현지화·SEO·문서화가 필요한지 (c) 정본의 명시 제외·확정 절과 충돌하지 않는지 판정하고, 계약으로 굳혀야 할 항목의 문안을 제안한다. 특히 **출력 파일명**은 기존 4모드 관례(`pdfWorkerClient`·organize 결과 이름)와 정합해야 한다.
12. **기록·범위 밖 발견**: CHANGELOG·review-notes U4-3 절 ↔ 로그 · 「범위 밖 발견」 각 항 판정(정본 미정의 임의 결정이 코드에 있는지 — 특히 단계 경계 처리·문구 신설·기본값).

## 3. 판정 형식
| 항목 | 판정([통과]/[결함]/[미검증]) | 재현 명령·출력 | 수정 지시 문안 | · 심각도 · 마지막 **[검수 통과] / [수정 후 재검수]**. 산출물 `/tmp/worklazy-u4-3-review/REPORT.md`.
