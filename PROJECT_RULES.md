# PROJECT_RULES.md — 전 에이전트 공통 규칙 정본

> 각 규칙에는 **이름**이 붙어 있다. 다른 문서·코드·커밋에서 규칙을 가리킬 때는 번호가 아니라 **이름**으로 쓸 것 — 번호는 규칙이 늘거나 재배열되면 어긋난다.
>
> 이 파일은 세 에이전트(Claude · Codex · Gemini)가 공유하는 **유일한 공통 정본**이다. 역할별 지시와 각 에이전트에서 비롯된 교훈은 `CLAUDE.md` · `AGENTS.md`(Codex) · `GEMINI.md`에 각각 있다. 공통 규칙을 고칠 때는 이 파일만 고친다 — 역할 파일에 공통 규칙을 복제하지 않는다.

## Ⅰ. 이 프로젝트가 무엇인가

**1. Worklazy Tools** — 설치·로그인 없이 브라우저 안에서 파일 작업(Excel 병합 · Word/HWP 비교 · HWP 편집 · PDF 도구 · 이미지/비디오/오디오 스튜디오 · 텍스트·표 데이터 · 근무/시차/급여 계산 · 개인정보/보안/QR)을 처리하는 정적 웹 도구 모음이다. https://worklazy.net 에서 서비스한다.

**2. 백엔드 없음** — 사용자가 선택한 파일과 암호는 외부 서버로 전송하지 않고 현재 브라우저 탭 안에서만 처리한다. 이 약속은 제품의 정체성이다 — 이를 깨는 설계는 기능이 아니라 사고다.

## Ⅱ. 제품 규칙

**3. 「GitHub Pages 스택」** — 이 프로젝트는 GitHub Pages에서 구동 가능한 스택만을 사용한다. 서버 런타임·백엔드 API·서버 사이드 렌더링을 전제로 한 코드는 들어올 수 없다.

**4. 「현지화·SEO·AdSense 동시 검토」** — 새 기능을 추가하거나 기존 코드를 수정할 때는 해당 변경 범위의 한국어·영어 번역, SEO 메타데이터·정적 페이지·사이트맵·FAQ 영향, Google AdSense 정책·광고 배치·로딩 성능·광고 제외 격리 경로 영향을 반드시 검토하고 필요한 코드·문서·검증을 함께 반영한다. 도메인·ads.txt·검색 등록 등 배포·검색·AdSense 확인 절차의 정본은 [`docs/PUBLISHING_CHECKLIST.md`](docs/PUBLISHING_CHECKLIST.md)다 — 절차가 바뀌면 이 규칙이 아니라 그 문서를 갱신한다.

**5. 「내부 구현 비노출」** — 도구 기능 화면과 진행·오류 메시지에는 Worker·런타임 같은 내부 구현 명칭, 광고 스크립트의 로드·제외 상태, 격리 문서·경로, 디버그 상태나 원시 예외 메시지를 최종 사용자에게 노출하지 않는다. 필요한 안내는 사용자의 행동과 결과 중심으로 한국어·영어 현지화한다. 다만 개인정보처리방침·동의 화면의 필수 고지는 예외로 한다.

## Ⅲ. 일하는 방식

**6. 「권한을 묻지 말 것」** — 지시했다면 그 일에 필요한 행위까지 허락한 것이다.

**7. 「작업 기록」** — 기록은 두 파일로 나눈다(2026-09-02 사용자 결정). **`CHANGELOG.md`에는 코드에 일어난 변경을 간결히**(무엇이 바뀌었나 — 한 작업당 몇 줄), **검토 과정에서 산출된 사고의 결과물 — 판정·기각 사유·실측 수치·가설 검증 — 은 [`docs/review-notes.md`](docs/review-notes.md)에** 남긴다. 둘 다 자신의 이름(Claude · Codx · Gemini)으로 서명한다. CHANGELOG에 검토 서사를 섞지 말고, review-notes에서 해당 CHANGELOG 항목을 알아볼 수 있게 작업 단위 제목을 붙인다. 커밋 메시지는 저장소 관례(영어 한 줄 요약)를 따른다.

**8. 「검증은 실행이다」** — 완료 기준의 검증 명령을 실제로 실행하고 출력을 기록한다. "통과했을 것"은 완료가 아니다. 이 저장소의 기본 검증 명령: `npm run build` · `npm run test:unit` · 변경 범위에 해당하는 스모크 테스트(`test:browser` · `test:office` · `test:new-tools` · `test:utilities` 등) · 정적 산출물 검증 `npm run test:static`.

**9. 「생성물 직접 수정 금지」** — `dist/` · `public/vendor/**`(pyodide · tesseract · zetaoffice · video-studio 런타임) · `THIRD_PARTY_NOTICES.md` · `scripts/generate-*.mjs`가 만들어내는 정적 페이지·소셜 이미지 산출물은 손으로 고치지 않는다. 정리·수정은 생성·벤더링 스크립트(`scripts/`)와 그 입력에서만 한다. 벤더 자산은 **고정 스냅샷의 크기·SHA-256 검증을 통과한 것만** 채택하며 다른 응답이면 빌드를 중단한다 — 스냅샷 정본(해시표)과 운영 절차는 [`docs/OFFICE_EDITOR_ASSETS.md`](docs/OFFICE_EDITOR_ASSETS.md)다. 스냅샷을 올릴 때는 이 규칙이 아니라 그 문서의 해시표를 갱신한다.

**10. 「작업지시서 관리」** — 작업 중에는 깃 추적에서 제외한 `docs/jobs/todo`의 오프라인 문서로 관리한다. 작업 묶음이 종결되면 핵심 결과는 `CHANGELOG.md`, 살아 있는 후속은 `docs/backlog.md`에 반영한 뒤 jobs 원문을 오프라인 아카이브로 묶는다. jobs 원문과 아카이브는 깃에 커밋하지 않는다. (`docs/CODEX_WORK_ORDER*.md`는 이 체계 도입 전의 기록이다 — 새 작업지시서는 이 방식을 따르지 않는다.)

**11. 「실행 게이트」** — 작업지시서를 실행하는 누구에게나 적용된다.

**기준 해시 게이트** — 다른 세션이 작성한 작업지시서를 실행할 때 문서가 명시한 기준 해시와 현행 `HEAD`를 먼저 대조한다. 전제를 깨는 변경이 있으면 실행 전에 충돌과 영향을 작업지시서에 기록하고 사용자에게 보고한다.

**열린 계획서 충돌 검사** — 착수 전에 `docs/jobs/todo`의 열린 계획서를 스캔해 같은 코드 표면의 상반된 지시가 있는지 확인한다. 충돌이 있으면 어느 쪽도 조용히 실행하지 말고 최신 사용자 결정에 따라 하나의 정본으로 합친다.

**광역 금지 계약 — 저장소 전체 재귀 탐색과 최소 허용목록** — 어떤 호출·의존·파일군을 광역으로 금지하는 계약(예: 광고 제외 격리 경로에서 광고 스크립트 금지, 도구 화면에서 원시 예외 노출 금지)을 구현하거나 감사할 때는 실행 가능한 확장자별 repo-wide 재귀 탐색과 명시적 최소 허용목록을 함께 둔다. 특정 현행 디렉터리만 검사해 새 하위 경로를 놓치지 않는다. 문서·fixture·generated·vendor 예외는 목적과 소유자를 적어 분리하며, 광역 wildcard 예외나 현재 glob 결과로 기대 허용목록을 자기 생성하는 방식은 금지한다.

## Ⅳ. 협업 구조 — 세 에이전트

**12. 「역할 분담」** — 세 에이전트가 역할을 나눈다. 상세 지시와 각자에서 비롯된 교훈은 각자의 파일에 있다.

- **Claude** (`CLAUDE.md`) — 오케스트레이터: 계획 설계 · 작업지시서 작성 · 감사 · 판정 · 취합. 직접 수정은 문서·계획서와 한두 줄 통합 수정까지.
- **Codex** (`AGENTS.md`) — 코드의 유일한 작성자: 구현 · 수정 · 테스트. 감사 소견에 대한 반박자.
- **Gemini** (`GEMINI.md`) — 대규모 컨텍스트 분석 · 시각적 UI 검토 · 대안 제시. Claude/Codex 가 막혔을 때의 신선한 시각.

**13. 「증거 없는 반박은 무게 0」** — 감사·리뷰·반박에서 판정의 화폐는 **실행한 명령과 그 출력**이다(통과하는 테스트, grep 결과, 재현 로그). 논증만 있는 주장·반박은 판정에 반영하지 않는다.

**14. 「Gemini 산출물은 단서다」** — Gemini 가 낸 수치·경로·해시·실행 지시는 Claude 또는 Codex 의 실측 검증을 거치기 전에 계획·코드에 편입하지 않는다.

**15. 「코드 단일 작성자」** — 코드 수정은 Codex 로 귀속한다. Claude·Gemini 가 코드 결함을 찾으면 직접 고치지 말고 수정 지시서(대상 파일 · 근거 · 완료 기준 검증 명령)로 전달한다. 한 표면에 작성자가 둘이면 스타일이 갈라지고 CHANGELOG 귀속이 흐려진다.

**16. 「계획 정본화」 — 작업은 정본화된 계획서로 시작한다** — 모든 실질 작업은 계획에서 시작한다.

**발동 조건: 사용자가 `!계획!` 이라고 명시하기 전에는 계획 수립을 시작하지 않는다.** 그 전의 대화는 브레인스토밍이다 — 사용자가 던진 말에 무턱대고 계획을 짜기 시작하지 말고, 의견·평가·대안으로만 답한다. 계획서 초안 작성은 `!계획!` 이후에만 한다.

절차(`!계획!` 발동 후):

1. Claude 가 계획 초안을 설계한다.
2. Codex 와 **반박 왕복**으로 보완한다 — 구현자가 계획 단계에서부터 "코드를 모르고 하는 소리"를 걸러낸다(「증거 없는 반박은 무게 0」 규칙 적용). 왕복은 이견이 해소될 때까지 반복한다.
3. 필요 시 Gemini 의 보충 의견을 받아 반영한다(「Gemini 산출물은 단서다」 규칙 적용).
4. **착수 조건은 Claude 와 Codex 간 이견이 남아 있지 않은 지점 도달이다.** 이견이 좁혀지지 않으면 정본화하지 말고 쟁점을 정리해 사용자 결정으로 넘긴다. 이견 0 에 도달하면, 최종 작업 **직전**에 대화 형식으로 흩어진 결정 내역을 하나의 작업계획서로 정리해 **정본화**한다 — `docs/jobs/todo` 오프라인 문서로, 기준 해시·완료 기준 검증 명령·명시 제외 목록을 포함한다. **대화 스레드는 정본이 아니다.**
5. 착수 지시는 정본화된 계획서로 Codex 에게 전달한다. 디스패치에는 `PROJECT_RULES.md` 선독 지시를 포함한다(Codex 는 임포트 기제가 없다).

**17. 「부하 분산과 대체 경로」** — 누가 무거운 일을 하는가, 한도가 바닥나면 누가 대신하는가.

- 다관점 검증·팬아웃 감사처럼 **서브에이전트를 헤비하게 돌리는 작업**은 일부 또는 전부를 Gemini 호출로 수행한다.
- **외부 벤더 런타임(pyodide · tesseract · zetaoffice · ffmpeg 계열) 최신화, AdSense 정책·검색엔진 가이드라인 등 외부 문서 확인을 위한 인터넷 검색**은 Gemini 가 담당한다.
- **Gemini 가 세션 한도 초과 상태면**: 서브에이전트 역할은 Claude 가 **Opus 계열 모델을 호출**해 대신 시키고, **인터넷 검색 등 조사 역할은 Codex 가 임시 수행한다**(검색 위임 조건은 Gemini 한도 초과뿐이다 — Claude 한도와 무관).
- **Claude 자신의 한도가 많이 부족하면**(**5시간 한도 20% 이하 또는 주간 한도 10% 이하**) **Opus 로 전환해 직접 수행**한다.

**19. 「배포 전 로컬 시각 검수」** — UI에 영향을 주는 변경은 main push(=Pages 배포) 전에 반드시: ① **네이버·구글 애널리틱스와 광고 추적 코드를 제외한 로컬 빌드**를 띄우고 ② **브라우저로 변경 영향 페이지를 직접 열어** 깨진 곳이 없는지 확인한 뒤에만 배포한다. **브라우저 시각 검수는 Gemini 담당**(스크린샷 순회·판정 보고), 최종 판정은 Claude·Codex 실측 교차로 한다. 여기서 "깨짐"은 레이아웃 파손·문구 잘림만이 아니라 **글자가 한쪽으로 쏠리거나 세로로 낙하하는 정렬 붕괴, 토글 스위치의 트리거(썸)가 스위치 트랙 밖으로 이탈하거나 상하 기준이 틀어지는 컴포넌트 내부 오정렬까지 포함**한다. 검수 통과 전 push 금지. (2026-09-04 사용자 결정 — shadcn 단계별 직배포가 문서 비교 등 라이브 전역 파손을 낸 사고가 배경. 자동 화면 비교 표본만으로는 대체 불가 — 눈으로 보는 검수가 게이트다.)

**18. 「커밋·업로드·배포는 Codex」** — 이 프로젝트의 git 커밋 · push(원격 업로드) · 배포는 **전부 Codex 가 수행한다**. Claude·Gemini 는 커밋·push 하지 않는다 — 변경을 만들었으면 워킹트리에 남기고 커밋·push 를 Codex 에 위임한다. `main` push 가 GitHub Actions 의 Pages 배포를 트리거하므로, push 전에 「검증은 실행이다」 규칙의 검증 명령을 통과시키고, 배포·도메인·광고 영향이 있는 변경은 [`docs/PUBLISHING_CHECKLIST.md`](docs/PUBLISHING_CHECKLIST.md)의 해당 항목도 확인한다.
