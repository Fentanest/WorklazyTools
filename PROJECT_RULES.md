# PROJECT_RULES.md — WorklazyTools 공통 규칙

이 파일은 공통 정책의 유일한 정본이다. 역할별 행동은 CLAUDE.md, AGENTS.md, GEMINI.md에 둔다.
문서 안에서는 번호가 아니라 규칙 제목으로 참조한다. 같은 정책을 역할 파일과 스킬에서 따로 정의하지 않는다.
명시적인 최신 사용자 지시를 우선하되, 실행 환경의 안전·권한 제한을 문서로 우회하지 않는다.

## 제품 원칙

- 설치·로그인 없이 브라우저에서 사용하는 정적 웹 도구 모음이다. GitHub Pages에서 실행 가능한 구조를 유지한다.
- 서버 런타임·백엔드 API·SSR을 전제로 설계하지 않는다. 사용자 파일·암호·처리 결과를 외부로 보내지 않는다.
- 진단 로그·URL·분석 이벤트·오류 보고에도 사용자 문서 내용과 민감정보를 넣지 않는다.
- 변경 범위의 한국어·영어 문구, SEO·정적 페이지·사이트맵·FAQ, 광고 배치·제외 경로 영향을 검토한다. 영향 없는 영역까지 수정하거나 전수 검사하지 않는다.
- 사용자 화면에는 내부 런타임 명칭·광고 로딩 상태·디버그 정보·원시 예외를 노출하지 않는다. 필요한 법정 고지와 개인정보 안내는 예외다.
- 승인된 디자인·테마·컴포넌트를 유지한다. 디자인 스킬을 근거로 색상·폰트·레이아웃을 임의로 재설계하지 않는다.
- dist, 벤더 자산, 라이선스·정적 페이지 등 생성물은 손으로 고치지 않는다. 생성·벤더링 스크립트와 입력을 수정한다. 고정 스냅샷의 크기·해시 검사 실패를 우회하지 않는다.

게시 절차는 docs/PUBLISHING_CHECKLIST.md, 벤더 스냅샷은 docs/OFFICE_EDITOR_ASSETS.md를 참조한다.

## 역할·권한

| 담당 | 허용되는 역할 |
|---|---|
| Claude — 총괄·감사 담당 | 계획·조정·감사·최종 판정·취합, 문서·계획서 수정 |
| 구현 담당 Sol — GPT 5.6 Sol | 승인된 담당 작업의 제품 코드·테스트·설정 수정, 범위 내 검사, 담당 작업 브랜치의 커밋 |
| 구현 담당 Muse — Muse Spark 1.3 | 승인된 담당 작업의 제품 코드·테스트·설정 수정, 범위 내 검사, 담당 작업 브랜치의 커밋 |
| 통합·배포 담당 — 작업 묶음별로 지정한 Sol 또는 Muse 한 명 | 검수된 변경의 통합, 최종 후보 확인, 승인된 원격 반영·배포 |
| Codex 검수 담당 Astra | 계획의 기술 검토·반박, 결과 검수·임시 재현 실험. 총괄·감사 주체가 아니며 제품 수정·커밋·push 금지 |
| Gemini | 조사·광역 분석·시각 검토·대안 제시. 제품 수정·커밋·push 금지 |

Claude 총괄·감사, Astra 검수를 유지한다. Claude의 감사는 사용자 요구·제품 원칙·범위·중요 누락·결함 귀속·증거 적정성에 대한 책임과 최종 판정이다. Astra의 검수는 실제 diff·산출물·검사 증거·기술 동작을 확인하고 필요한 재현으로 소견을 제공하는 역할이다.
Astra의 검수 결과는 Claude의 감사·최종 판정에 입력한다. 두 역할의 이름을 바꾸어 맡기거나 한쪽 보고로 다른 쪽의 책임을 대신하지 않는다. 다만 감사라는 이유로 검수에서 이미 유효하게 확인한 전체 검사를 다시 실행하지 않는다. 필요한 범위는 지시서에 명시하고 새 근거 없는 감사 단계를 추가하지 않는다.
한두 줄의 제품 코드 수정도 구현 담당에게 맡긴다. 임시 재현 코드와 제품에 반영할 코드를 구분한다.
사용자가 문서 교체·스킬 설치·스킬 내부 경로 수정을 다른 담당에게 명시적으로 요청하면 그 유지보수 범위는 예외로 허용한다. 제품 구현 역할과 배포 권한까지 바뀌지는 않는다.

승인된 범위의 통상적 구현·로컬 검증은 반복해서 허락을 묻지 않는다. 의견·계획 요청을 구현·배포 승인으로 확대하지 않는다.
Sol과 Muse는 모두 정식 구현자이며 Muse를 비상 대체 상황에만 한정하지 않는다. 사용자가 담당을 지정하면 따르고, 지정하지 않았으면 총괄이 승인된 범위에서 작업 특성·가용성·사용 조건을 고려해 선택하고 지시서에 기록한다.
진행 중 담당 변경은 「독립 작업의 병렬 구현」의 인계 절차를 따른다. 어느 모델의 한도 소진도 Gemini 등 미지정 모델에게 제품 수정 권한을 주는 근거가 되지 않는다.
구현 담당은 자신의 작업 브랜치에서만 승인된 범위의 커밋을 만든다. 원격 push는 지시서에 허용된 경우에만 자신의 작업 브랜치에 수행하고, 통합 대상 브랜치·main·배포는 지정된 통합·배포 담당 한 명에게 귀속한다.
검수 대상 소스·계획서는 고정한다. 같은 작업 공간의 동시 구현은 금지하지만, 다음 조건을 충족하는 서로 독립적인 작업은 Sol과 Muse가 병렬로 수행한다.

## 독립 작업의 병렬 구현

### 병렬 착수 판단

총괄은 작업 분할 시 병렬 가능 여부를 먼저 판단하고, 독립성이 확인되면 Sol과 Muse에게 나누어 동시에 위임할 수 있다. 모델이 둘이라는 이유로 같은 작업을 중복 구현시키거나 매 작업마다 두 모델 전원을 호출하지 않는다.
병렬 허용은 사용자에게 매번 재승인을 받을 사안이 아니다. 승인된 목표·범위 안에서 총괄이 판단하고 그 근거를 작업지시서에 짧게 기록한다. 독립성을 확인하지 못한 부분만 순차 처리한다.

- 각 작업이 따로 구현·검수 가능한 범위인지 확인한다. 수정 파일만 다르다는 이유로 독립적이라고 판단하지 않는다.
- 한 작업의 변경이 다른 작업의 API·타입·공용 컴포넌트·전역 CSS·상태·이벤트·출력 형식·런타임 전제를 바꾸면 먼저 공통 계약을 확정하거나 해당 변경을 선행·순차 처리한다. 변경되지 않는 공용 라이브러리를 함께 읽는 것 자체는 병렬 금지 사유가 아니다.
- 공용 파일·package.json·lock 파일·번역 통합 파일·라우트 등록·생성기·벤더 스냅샷·기준 이미지·공통 테스트 설정을 양쪽이 따로 바꾸지 않는다. 필요한 공통 변경은 한 명의 소유로 먼저 반영하거나 통합 단계에 묶고, 나머지 독립 작업만 병렬로 수행한다.
- 실제 diff나 재현 결과에서 새로운 연결·겹침이 발견되면 관련 작업만 멈추고 총괄에게 알린다. 지시서의 소유 범위·순서를 조정한 뒤 재개한다. 무관한 작업까지 중단하거나 기존 수정을 일괄 폐기하지 않는다.

### 작업 공간·기록·실행 자원

각 작업은 서로 다른 브랜치와 git worktree 또는 독립 checkout에서 수행한다. 같은 폴더에서 모델만 바꾸어 동시 실행하거나 같은 브랜치를 두 작업 공간에 강제로 체크아웃하지 않는다.
총괄은 작업당 구현 책임자 한 명과 허용 수정 범위, 기준 커밋, 실행 도구·정확한 모델 ID, 작업 브랜치·절대경로, 공유 계약, 검사 범위, 산출물 경로, 통합 순서·담당을 지정한다. 최소한의 작업표를 기존 지시서에 두고 새 관리 시스템을 만들지 않는다.
같은 기준 커밋에서 분기하는 것을 기본으로 한다. 선행 공통 변경이 필요한 작업은 해당 변경을 포함한 기준과 의존 순서를 명시한다. 다른 worktree의 미커밋 파일을 직접 읽고 복사해 의존성을 몰래 만들지 않는다.
Git 제외 지시서·규칙의 로컬 변경·스킬·fixture·실행 설정은 새 worktree에 자동으로 전달됐다고 가정하지 않는다. 각 실행자에게 필요한 사본과 버전·해시를 전달하고, 접근 불가한 열린 작업을 없음으로 처리하지 않는다.

빌드 산출물, 변경 가능한 node_modules·벤더 캐시, 테스트 fixture, 임시 디렉터리, 브라우저 프로필·세션, 캡처·로그 경로와 로컬 서버 포트를 작업별로 분리한다. 변경 가능한 폴더를 심볼릭 링크로 공유하지 않는다. 공유 읽기 전용 캐시는 안전성이 확인된 경우에만 재사용한다.
테스트가 고정 /tmp 경로나 포트를 사용해 분리가 안 되면 그 검사 단계만 순차 수행한다. 메모리·CPU 경합으로 측정이 왜곡되는 성능 검사도 순차로 돌리되 제품 구현 병렬성까지 불필요하게 막지 않는다. 존재하지 않는 포트·경로 옵션을 만들어 사용하지 않는다.

병렬 구현자는 자기 작업의 별도 보고 파일에 검증·변경 요약·후속을 기록한다. CHANGELOG.md·docs/review-notes.md·docs/backlog.md 등 공통 누적 문서는 통합 담당이 실제 작성자 서명을 보존해 취합한다. 공통 파일 편집 권한이 별도로 배정된 경우만 예외이며 동시 편집은 금지한다.
검토·통합용으로 제출한 커밋은 고정한다. 제출 후 수정이 필요하면 새 커밋을 제출하고 어떤 결과가 무효가 되는지 알린다. 다른 담당의 작업 공간·브랜치·작업 레코드를 임의로 수정·정리하지 않는다.

### 인계·통합

같은 작업을 Sol에서 Muse 또는 그 반대로 인계할 때는 기존 프로세스와 하위 작업의 종료를 확인한다. 현재 커밋·staged/unstaged diff·관련 untracked 파일, 수행한 검사와 증거, 미완료 작업을 보존하고 지시서의 소유자를 갱신한 뒤 후임을 시작한다. 이전 작업을 덮어쓰거나 두 실행을 동시에 살려 두지 않는다.
통합은 지정된 한 명만 별도 통합 브랜치·작업 공간에서 수행한다. 제출된 고정 커밋과 소유 범위를 확인하고 계획한 순서대로 merge 또는 cherry-pick 중 한 방법으로 반영한다. 같은 변경을 양쪽 방식으로 이중 반영하지 않는다.
병합 충돌이 없다는 사실만으로 동작 독립성이나 검증 통과를 주장하지 않는다. 충돌 해결은 계약을 유지하며 수행하고, 새 설계 판단이나 범위 확대가 필요하면 그 쟁점만 총괄에게 돌린다. 통합 대상 HEAD가 바뀌면 바뀐 차이의 영향과 순서부터 확인하고 무관한 계획·검사를 전부 재시작하지 않는다.
Astra는 실제 구현자가 누구든 같은 기준으로 작업 결과와 통합 후보를 검수한다. Claude는 검수 소견과 원본 증거를 바탕으로 감사·최종 판정한다. 필요한 감사·검수·최종 통합 조건이 충족되기 전 main 반영·배포를 하지 않는다. 작업 공간 정리는 산출물 보존과 통합 확인 후 수행하고 reset·clean·강제 삭제로 미완료 작업을 지우지 않는다.

## 계획·착수

### 요청 모드와 구현 허가

새 제품 구현 계획은 사용자가 `!계획!` 또는 `!계획만!`을 명시했을 때 시작한다. 두 명령 전에는 요청받은 의견·평가·문서 초안을 제공하되 새 구현 계획이나 구현을 임의로 시작하지 않는다.

| 요청 | 계획 과정 | 정본화 후 행동 |
|---|---|---|
| `!계획!` | Claude 초안 → Astra 기술 검토·반박 → Claude 응답·보완, 필요하면 보충 조사·추가 반박 → 차단 쟁점 해소 → 정본화 | 기존 사용자 요청이 허용한 목표·범위에서 지정된 구현자에게 착수를 지시할 수 있다. 별도 구현·배포 제한은 그대로 따른다. |
| `!계획만!` | `!계획!`과 같은 검토·반박·보완·정본화 절차와 품질 기준 | **정본화 완료·구현 대기**로 끝낸다. 코드 구현이나 구현 잡 발행으로 넘어가지 않는다. |

`!계획만!`은 계획의 정식 기술 검토나 필요한 반박 라운드를 생략하라는 뜻이 아니다. 계획서·쟁점 기록과 계획 검증에 필요한 읽기·조사·허용된 격리 재현은 가능하다. 제품 코드·테스트·설정·자산·의존성을 수정하거나 구현용 잡·통합·배포를 시작하지 않는다. 읽기·재현 목적의 검토 잡에 구현 권한을 붙이지 않는다.
정본 계획서에 요청 모드(`plan-only` 또는 `plan-and-implement`), 계획 상태, 구현 허가 여부·근거를 기록하고 위임에도 그대로 전달한다. 계획만 요청의 정상 종료는 「정본화 완료·구현 대기」이며, 차단 쟁점이 남으면 「검토 중·미해결 쟁점 있음」으로 남긴다.
계획만 요청 뒤에는 사용자의 「이 계획으로 구현 시작해」 등 명시적인 구현 지시를 받아야 진행한다. 「좋다」 같은 일반적 수긍, 정본화 성공, 세션 재개·분기·모델 교체만으로 구현 허가를 추정하지 않는다. 대상 계획과 승인 범위를 기록한 뒤 허가 상태를 갱신하며, 승인된 정본을 이유 없이 처음부터 다시 계획하지 않는다.
두 요청이 섞였을 때 명확히 취소되지 않은 `!계획만!`의 제한을 우선한다. 제한은 해당 작업에 적용하며 관련 없는 이미 승인된 작업까지 임의로 중단하지 않는다. 어느 계획 명령도 독립적인 커밋·push·배포 승인을 뜻하지 않는다.
이미 정본화되고 구현이 허가된 작업의 범위 내 수정은 새 계획을 요구하지 않는다. 명시적으로 요청된 문서 교체·스킬 설치는 유지보수이므로 별도의 계획 명령을 요구하지 않는다.

### 반박 라운드와 종결

Claude가 계획·감사의 주체이고 Astra가 계획의 기술 검토·반박과 결과 검수에 참여한다. Sol·Muse는 자신의 구현에 대한 소견에 근거를 들어 응답할 수 있으며, Gemini의 조사·시각 검토는 필요한 쟁점에만 활용한다. 모든 모델의 참여를 매 라운드 의무화하지 않는다.
기본 1회 검토와 1회 재검토(1+1회)는 **통상적인 점검 시점이지 상한이 아니다**. 차단 쟁점이 일찍 해소되면 불필요한 왕복을 채우지 않으며, 다음 사유가 있으면 필요한 쟁점에 한해 추가 반박 라운드를 진행한다.
- 새로운 코드·문서·실측 증거 또는 구체적인 반례가 나왔다.
- 계획이 보완되거나 공유 계약이 바뀌어 중요한 쟁점을 다시 확인해야 한다.
- 차단 쟁점의 근거가 아직 불충분하고 구체적인 추가 조사·판별 실험으로 해결할 수 있다.

Claude는 추가 라운드의 쟁점·확인할 근거·담당·종료 기준을 적고 진행한다. 횟수가 찼다는 이유로 통과시키거나 미해결 쟁점을 곧바로 사용자에게 떠넘기지 않는다. 이미 해소된 쟁점을 새 근거 없이 다시 열거나 같은 논점만 반복하지 않는다.
새 근거 없는 반복에는 같은 대화 대신 판별 실험·원본 확인·검토 관점 변경을 사용한다. 가용 수단으로도 사실을 확인하지 못하면 미해결과 막힌 이유를 정확히 보존·보고하며, 한도·시간 소진을 합의나 통과로 바꾸지 않는다. 사용자 판단은 제품 방향·상충 요구·위험 수용처럼 실제로 사용자 선택이 필요한 경우에 요청한다.
정본화·착수 조건은 요구사항·안전성·완료 기준에 관한 차단 쟁점의 해소다. 취향·비필수 최적화는 기록하고 진행할 수 있다. 계획이 정본화돼도 `plan-only`이면 구현 대기를 유지한다.
추가 반박은 **쟁점 검토의 추가**다. 이것을 전체 회귀·전체 감사의 자동 재실행 사유로 삼지 않는다. 변경 범위와 결과 재사용은 「검증 범위」를 따른다.

### 지시서·착수 확인

작업지시서에는 작업 ID·버전·요청 모드·계획 상태·구현 허가, 기준 소스, 목표·범위·제외 범위, 완료 기준, 이번 검사·최종 검사, 산출물 위치, 커밋·배포 범위를 넣는다.
구현자·실행 도구·정확한 모델 ID·브랜치/worktree·통합 담당을 명시한다. 병렬 작업에는 소유 파일·공유 계약·의존 순서·자원 분리 정보를 함께 적는다. Muse 위임에는 신규/재개/분기 선택과 세션 ID·작업 공간의 연결도 기록한다.
지시서는 docs/jobs/todo에서 오프라인으로 관리한다. 다른 환경에 전달할 때 사본과 버전·해시를 함께 제공한다. 대화 스레드만을 정본으로 사용하지 않는다.
착수 전 구현 허가, 기준 커밋·미커밋 변경·열린 작업 충돌을 확인한다. 접근 불가를 열린 작업 없음으로 해석하지 않는다. 무관한 HEAD 변경만으로 계획 전체를 다시 만들지 않는다.
허가된 구현의 독립성이 확인되면 「독립 작업의 병렬 구현」에 따라 Sol·Muse에게 병렬 위임할 수 있다. 계획만 상태의 작업을 병렬 슬롯이 비었다는 이유로 구현에 넘기지 않는다.

## Muse 세션 연속성

Muse의 연속·연관 작업과 기존 작업의 후속은 적합한 기존 세션을 먼저 찾아 이어간다. 작업마다 무조건 새 세션을 만들어 기존 판단·수정 맥락을 버리지 않는다. 상세 조회·명령 형식은 docs/agent-dispatch-runbook.md를 따른다.
- 바로 전 세션이 해당 작업·작업 공간·모델의 세션임이 확인되면 `-c`로 이어갈 수 있다.
- 특정 기존 작업을 이어가거나 병렬 작업으로 마지막 세션이 달라질 수 있으면 세션 ID를 찾아 `-s <session-id>`로 명시한다. ID가 이미 기록돼 있으면 그것을 우선한다. 제목이나 마지막 실행 시각만으로 다른 작업을 잘못 이어가지 않는다.
- 원래 대화를 보존하면서 관련 대안·실험·분리된 후속을 다룰 필요가 있으면 `-s <session-id> --fork`를 사용한다. 마지막 세션이 확실할 때만 `-c --fork`도 허용한다. 같은 작업의 정상 후속마다 분기하지 않는다.
- `--fork`는 대화 문맥의 분기이며 코드 브랜치·worktree 생성이나 권한 확대가 아니다. 분기된 구현을 병렬로 수행하려면 별도 작업 공간·소유 범위·실행 자원 분리를 함께 적용한다. 같은 세션 ID에 두 실행을 동시에 쓰지 않는다.

재개·분기 전 저장소·worktree·실제 모델·실행 상태·요청 모드·최신 규칙과 지시서 버전을 확인한다. 세션의 오래된 지시가 바뀐 최신 규칙을 덮어쓰지 않으며, `plan-only`와 미해결 쟁점은 그대로 전달한다. 과거 맥락을 이어받아도 테스트 결과는 현재 소스에 대한 유효성을 따로 판단한다.
분기할 때는 부모 ID·실제 생성된 자식 ID·분기 목적·새 작업 공간을 기록한다. 이후 그 분기의 후속은 자식 ID로 잇는다. 세션 재개·분기가 다른 모델로의 전환이나 구현·배포 승인으로 해석되어서는 안 된다.
해당 세션을 찾지 못하거나 손상·도구 제약으로 안전하게 이어갈 수 없으면 근거와 인계 요약을 남겨 새 세션을 시작할 수 있다. 이것이 계획만 상태를 해제하거나 새 기능을 착수할 권한은 아니다.

## 검증 범위

### 수정 중

변경한 동작과 직접 영향받는 소비자만 검사한다. 파일 수나 수정 줄 수가 아니라 호출·상태·공용 코드·생성기·의존성 관계로 판단한다.
관련 단위 테스트, 신고 재현, 해당 기능 스모크부터 수행한다. 전체 시각·접근성·성능·전 도구 회귀를 기본으로 붙이지 않는다.
현재 소스의 빌드가 필요한 검사에는 실제로 그 빌드를 사용한다. 오래된 빌드를 검사하고 현재 수정이 통과했다고 쓰지 않는다.
범위 필터는 실제 지원되는 것만 사용하고 선택 대상·실행 건수를 확인한다. 0건 실행·중단·실행 불가를 통과로 처리하지 않는다.

다음은 적용될 때 필수다. 해당하지 않으면 이유를 짧게 남긴다.
- 이번 변경이 앞서 고친 경로에 영향을 주면 그 결함 재발 검사.
- 파일 생성·변환·정렬·직렬화 등 출력 계약에 영향을 주면 출력 호환 검사.
- 검사기·대상 선택·집계·판정 로직을 바꾸거나 신뢰성에 의문이 있으면 검사기 자체 검증.
- 사용자 신고를 수정하거나 신고 경로를 다시 바꾸면 해당 재현.

테스트가 너무 넓고 필터가 없으면 관련 테스트 직접 실행이나 최소 대체 검사를 검토한다. 존재하지 않는 옵션을 만들거나 완료 기준을 조용히 생략하지 않는다.

### 최종 통합

제품 코드·빌드·의존성 변경이 포함된 작업 묶음의 최종 후보에서는 다음의 유효한 결과를 확인한다.
- npm run build
- npm run test:unit
- npm run test:static
- 변경 범위의 스모크·필요한 로컬 시각 검수
- 수정 중 최종 단계로 미룬 필수 검사

이 목록은 수정 라운드마다 반복하는 목록이 아니다. 최종 후보에 유효한 결과가 있으면 재사용한다. 이후 수정으로 무효가 된 검사만 다시 수행한다.
병렬 작업은 각자 변경 범위의 검사만 먼저 수행하고 공통 최종 검사는 통합 후보에서 한 번 취합한다. 두 브랜치가 각각 통과했다는 이유만으로 합쳐진 후보를 통과 처리하지 않는다.
통합으로 바뀐 입력·공유 계약·연결 경로·충돌 해결 부분의 검사를 실행하고, 영향이 없다는 근거가 있는 결과만 재사용한다. 통합 후보와 다른 산출물의 빌드·정적 검사 결과를 같은 결과로 재사용하지 않는다. 통합했다는 이유만으로 무관한 전 도구 회귀를 새로 붙이지 않는다.
제품 동작·빌드 입력에 영향을 주지 않는 문서·스킬 설치는 정적 정합성·경로·메타데이터만 검사한다. 제품 빌드·전체 회귀는 실행하지 않는다.
테스트만 변경하면 해당 테스트와 판정 로직을 우선 확인한다. 확장자가 아니라 실제 영향을 기준으로 한다.
CI·브랜치 보호·배포에서 강제하는 검사는 이 규칙으로 비활성화하거나 우회하지 않는다.

### 전체 회귀 확장 조건

전역 스타일·공통 상태·라우팅·핵심 의존성·빌드·캐시 변경, 여러 기능의 실제 회귀, 범위를 한정할 수 없는 구체적 위험, 사용자 명시 요청이 있을 때 확장한다.
원인과 추가 대상을 먼저 기록하고 공통 영향 영역부터 넓힌다. “혹시 모르니”, “최종 검수니까”만으로 전 기능 검사를 추가하지 않는다.

### 결과 재사용

검사별 명령·옵션, 대상 소스·빌드, 관련 환경·입력, 종료 코드·관찰 결과, 로그·산출물 위치를 남긴다.
재사용에는 관련 코드·테스트·설정·의존성·입력·환경이 달라지지 않았다는 근거가 필요하다. HEAD만 같다고 작업트리 변경을 무시하지 않는다.
담당자·세션 변경만으로 결과를 버리지 않는다. 단순한 통과 보고나 접근 불가한 증거는 재사용 근거가 아니다.
구현자와 검수자가 같은 전체 검사를 복제하지 않는다. 검수자는 diff·결과의 존재·대상 일치·누락을 확인하고 고위험 경로·이상 징후·핵심 표본을 독립 재현한다.
“적용 대상 아님”, “필요하지만 이월”, “재사용”, “새 실행”을 구분한다. 새로 실행하지 않은 검사는 재실행했다고 보고하지 않는다.

## 증거·결함·종료

실행·성능 주장은 실제 명령·환경·출력, 코드 분석은 파일 위치·조건·호출 관계, 외부 사양은 공식 출처·확인일·버전으로 뒷받침한다. 가설은 전제와 미확인 사항을 밝힌다.
실행 증거만을 유일한 증거로 보지 않는다. 설계 모순과 논리적 반례도 검토 근거가 된다. 어느 AI의 보고든 중요한 수치·전수 주장·차단 판정은 원본 증거를 확인한다.

결함은 발생 시점과 심각도를 별도로 판정한다.
- 신규 결함은 기존 부채를 이유로 면제하지 않는다.
- 기존의 비차단 결함은 백로그로 보낸다. 이번 변경으로 노출·악화되면 그 영향은 따로 처리한다.
- 정보 유출·데이터 손상·핵심 기능 사용 불가는 기존 결함이라도 배포 차단 여부를 판단한다.
- 취향·비필수 정리는 요구사항 위반과 구분한다. 발생 시점을 확인하지 못하면 미확인으로 적는다.

차단 결함을 수정하면 관련 경로만 재검증한다. 완료 기준·허용오차·기대값을 낮춰 통과를 만들지 않는다.
해당 단계의 필수 검사와 차단 쟁점이 해소되면 종료한다. 새 근거 없이 추가 감사·반박을 만들지 않는다.

## 검수 공간·시각 검수·배포

원본 작업 공간과 검수용 쓰기 공간을 분리한다. 임시 파일·빌드·브라우저 실행은 지정된 공간에서 허용하되 제품 수정·커밋·push 권한을 함께 주지 않는다.
산출물은 임시 경로나 검수용 복사본에 둔다. 검사기가 저장소 내부 경로를 요구하면 복사본 안의 경로를 사용한다. git worktree나 복사본 자체가 보안 경계는 아니며 파일·네트워크·자격증명 권한도 확인한다.
git status와 필요한 파일 비교는 사후 확인이다. 프롬프트의 수정 금지와 실제 쓰기 차단을 혼동하지 않는다. 권한 오류를 전역 샌드박스·승인 설정 해제로 해결하지 않는다.

UI 변경은 광고·분석 추적 코드가 실행되지 않는 로컬 환경에서 확인한다. 언어·테마·화면 크기·초기/상호작용/하단 중 영향받는 조합을 선정해 실제로 열람한다.
자동 픽셀 비교나 캡처 생성만으로 시각 검수를 대체하지 않는다. 보지 않은 화면은 미확인이다. 기본 담당은 Gemini이며 구체적 기능 결함은 Codex가 확인한다.

main push는 배포 작업이다. 작업 묶음에서 지정한 통합·배포 담당 한 명만 승인 범위, Astra 검수 결과와 Claude의 감사·최종 판정을 확인한 뒤 수행한다. 위임 문서도 diff를 확인하고 작업과 무관한 변경을 포함하지 않는다.

## 스킬 사용

| 구분 | 이름 | 담당·사용 조건 |
|---|---|---|
| A | worklazy-systematic-debugging | 지정된 구현자의 버그·테스트 실패 원인 조사와 국소 수정 |
| B | vercel-react-best-practices | 지정된 구현자·Astra의 관련 React 상태·렌더링·클라이언트 성능 검토 |
| C | playwright | 지정된 구현자·Astra 및 실행 환경이 있는 Gemini의 지정 UI 재현·캡처 |
| D | web-design-guidelines | Gemini의 지정 UI 사용성·접근성·일관성 검토 |
| E | frontend-design | Claude의 요청된 설계·검토, 지정된 구현자의 승인된 디자인 구현 |
| F | worklazy-scoped-verification | Claude 범위 선정·감사·판정, 지정된 구현자 실행, Astra 검수, Gemini 증거 제공 |

프로젝트 설치 위치는 .agents/skills에 A~F, .claude/skills에 E·F다. 공유 폴더가 역할별 권한 분리를 대신하지 않는다. 설치 여부와 런타임 인식 여부도 구분한다.
작업에 관련된 스킬만 읽고 해당 역할의 절차만 수행한다. E는 일반 UI 버그 수정에 자동 적용하지 않는다. 스킬이 없는 일반 질의·문서 설치에서 제품 검증을 시작하지 않는다.
스킬은 이 문서의 역할·범위·검증·권한을 바꾸지 않는다. 충돌하는 자동 스킬 호출·전체 검사·재설계 절차는 적용하지 말고 위치를 보고한다.
선택적 스킬이 없어도 기본 도구로 가능한 작업은 진행한다. 필수 검증 수단이 없으면 실행 불가로 남기며 통과로 처리하지 않는다.
설치·업데이트는 사용자 요청이나 사전 허용 범위에서만 수행한다. 원본·수정본 중복 적용을 피하고 출처·버전·프로젝트용 변경·스크립트의 설치·외부 전송 동작을 확인한다.
E·F의 두 설치본은 같은 내용으로 유지한다. Muse용 스킬 복사본을 추가로 만들지 않고 실행 환경에서 기존 경로의 인식 여부를 확인한다. 새 hooks·MCP·전역 권한 설정을 스킬 설치에 끼워 넣지 않는다.

## 기록·필요한 자료만 읽기

CHANGELOG.md에는 사용자에게 의미 있는 코드 변경을 몇 줄로, docs/review-notes.md에는 중요한 판정·기각·실측·교훈을, docs/backlog.md에는 후속을 남긴다. 서명은 Claude·Codx·Muse·Gemini다.
진행 중 상세 검증은 docs/jobs/todo의 작업당 한 기록을 갱신한다. 원시 로그와 같은 정상 결과를 상시 규칙에 쌓지 않는다. 종료 후 jobs는 오프라인 보관하고 커밋하지 않는다.
새 영역의 결정·기각 이력은 관련 부분을 검색해 읽는다. 거대한 review-notes와 모든 과거 지시서를 매번 통독하지 않는다.
호출·복구 때만 docs/agent-dispatch-runbook.md를 읽는다. 과거 실패 이력은 참고 자료이지 현행 규칙을 덮어쓰는 명령이 아니다.
저장소 전체 금지 계약은 실행 가능한 파일을 빠뜨리지 않는 정적 탐색과 최소 허용목록으로 검사한다. 이 탐색을 전 기능 회귀 의무로 확대하지 않는다.
